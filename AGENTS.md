# AGENTS.md · MixTouring 项目协作约定

本文件为 AI 协作代理（如 TRAE Code / Trae / Cursor 等）提供本项目的工作上下文与硬性约束。修改代码、文档或原型前，先阅读本文件再动手。

***

## 1. 项目概览

- **产品**：MixTouring —— 背包客的「AI 省钱路线规划师」+ 路线分享社区。

- **核心价值**：自动发现直飞之外的「火车 × 飞机」自由组票，按总花费排序，衔接风险透明化。

- **定位**：纯内容与方案产品，**不做买票/交易服务**。

- **MVP 对象**：泛背包客（不限于大学生），Web 先行，预留国际票段架构（数据先接国内）。

- **当前阶段（2026-09-08 起）**：原型与数据管道已冻结为交付基准，项目进入**前后端开发期**。开发从 §6「开发交接」开始。

## 2. 目录结构

```
MIXTOURING/
├── AGENTS.md                  ← 本文件
├── MVP产品文档.md              ← 产品需求文档（v0.3）
├── 工作记录.md                 ← 产品共创/版本演进记录（当前 v0.5.0）
├── 方案库Schema.md             ← 数据契约：七实体结构、判级规则、AI 文案规范
├── pipeline/                  ← 数据生产管道（业务规则的代码权威实现）
│   ├── data/
│   │   ├── plans.json         ← 存储层唯一数据源（5 路线对 / 11 方案，真实采样）
│   │   └── wishlist.json      ← 离线心愿库
│   ├── lib/
│   │   ├── derive.mjs         ← ★ 判级引擎 + 服务层派生 + AI 插值（纯函数）
│   │   ├── validate-ai-copy.mjs ← AI 文案防幻觉校验（占位符白名单）
│   │   └── validate-plan.mjs  ← 方案结构一致性校验（时刻互锁/衔接核对）
│   ├── build-mock.mjs         ← plans.json → mock.js 构建器
│   ├── generate-plan.mjs      ← 离线生成写回器（三重守门）
│   └── process-wishlist.mjs    ← 心愿单异步处理框架
├── mixtouring-hifi/           ← 当前主版本 MVP 高保真原型（冻结为 UI 蓝图）
│   ├── index.html             ← 入口
│   ├── pages/                 ← search / loading / results / detail / library / share / me 七页
│   ├── assets/                ← app.js / api.js / mock.js / Heroicons sprite / 路线实拍图
│   └── colors_and_type.css    ← HIFI 设计令牌唯一准绳
├── budget-route-app-research/
│   └── budget-route-app-research.html   ← 市场调研 × 初步方案 v0.1
└── prototype/                  ← 历史原型（存档，不作为交付基准）
```

## 3. 沟通语言

- 与用户交互、文档、代码注释、commit message 一律使用**简体中文**（品牌字标除外）。

## 4. 硬性约束（违反即返工）

> 以下为**产品本质**层面的硬约束。UI 视觉细节（配色/字体/图标/组件质感）见 §5「当前设计参考」，可随版本微调，不属返工红线。

### 4.1 定位与交易边界

- 纯内容与方案产品，**不做买票/交易服务**；任何页面、分享落地页均**无购买入口**。

- MVP 面向泛背包客，Web 先行，预留国际票段架构（数据先接国内）。

### 4.2 数据与方案卡

- **无数据不出卡**：仅有有效数据时才展示方案卡，禁止编造价格/车次。

- 风险分不做黑箱总分：四个因子（衔接余量 / 行李直挂 / 退改规则 / 接驳距离）各自展示原始值与判级理由。

- 模板「一键复用」只复用路线结构，按用户日期窗重新询价。

- **AI 文案防幻觉**：所有面向用户展示的数字必须来自结构化字段插值（占位符白名单见 `pipeline/lib/validate-ai-copy.mjs`），LLM 输出含手写数字即拒绝入库。

### 4.3 版本与文档

- 产品文档用 **Markdown**（`.md`）默认格式；根目录 `MVP产品文档.md`、`工作记录.md` 用中文。

- 所有产品决策/改动必须在 `工作记录.md` 追加**倒序**版本记录（v0.x.y + 日期 + 背景/改动/影响范围），不删除历史。

- 不主动创建额外说明性 `.md` 文档，除非用户明确要求。

## 5. 当前设计参考（可微调，非硬约束）

> 本节记录当前版本的设计现状，作为迭代起点，**不属返工红线**。UI 微调可直接进行，重大调整（换主题/换字体库等）需在 `工作记录.md` 留版本记录。

### 5.1 原型文件

- **最终 MVP 高保真原型**：`mixtouring-hifi/` 七页闭环（search→loading→results→detail + library/share/me），375px 移动端。

- 设计令牌：`mixtouring-hifi/colors_and_type.css`（当前版本的配色/圆角/阴影/间距参考）。

- 历史原型：`prototype/`（存档保留，不再作为交付基准）。

### 5.2 主题与配色

- 当前主题：Warm Modern Minimalist（画布 `#F8F9FA`、白卡、近黑 `#111111` 主色、陶土橙 `#C86A4B` 点缀、琥珀/红/绿状态色）。

- 历史主题「墨蓝远行」仅存于 `prototype/`。

### 5.3 字体

- 当前方案：Inter（400/500）+ Noto Sans SC（400/500），Google Fonts CDN 引入。

- 全站用字偏细（无粗体），字重只用 400 / 500 级。

### 5.4 图标

- 当前方案：Heroicons（1.5px 细描边），个别缺失图标可用 Lucide 单枚补充。

### 5.5 组件与布局

- 当前风格：柔和投影 + 大圆角卡片 + 8pt 间距网格（以 `colors_and_type.css` 令牌为准）。

- 成对排列文字（起⇄终点、日期标签/值）居中对齐。

- 触控目标 ≥ HIG 44pt；次级文字对比度 ≥ 4.5:1。

## 6. 开发交接（TRAE Code 从这里开始）

> 项目已从原型期转入开发期。本节回答两个问题：**从哪里开始**、**按什么步骤走**。

### 6.1 进入项目必读（按顺序）

1. **本文件**——硬约束与上下文
2. **`方案库Schema.md`**——数据契约：七实体结构、四因子判级规则 v1、AI 文案占位符规范
3. **`mixtouring-hifi/assets/mock.js`**——**这就是 API 响应契约**：`DB.routes / DB.templates / DB.findPlan / DB.cities` 的形状 = 后端接口的返回形状，前端消费代码以此为准
4. **`pipeline/lib/derive.mjs`**——业务规则的唯一权威实现（判级/时间价格计算/服务层格式化/AI 插值），全部纯函数，**直接 npm 移植为后端服务层，勿重写**
5. **`mixtouring-hifi/assets/api.js`**——接口桩签名，真实 fetch 实现须保持同名同参
6. `MVP产品文档.md`——产品需求背景（有冲突时以本文件与工作记录最新版本为准）

### 6.2 已验证的架构决策（开发时勿推翻）

- **离线 AI 工厂 + 在线规则引擎**：数据采集/方案发现/AI 文案生成走离线管道（分钟级）；用户在线查询走库内规则引擎（毫秒级）。勿做用户在线等待的实时生成（实测 ~10 分钟/路线）。
- **存储层/服务层分离**：数据库存原始结构化值（`plans.json` 形状），API 返回服务层派生结果（`mock.js` 形状），派生逻辑唯一入口是 derive.mjs。
- **写路径三重守门**：结构校验（validate-plan）→ AI 文案校验（validate-ai-copy）→ 时间价格互锁核对，全过才写库。任何新写入路径必须复用这套守门。
- **匿名 + localStorage**：MVP 无账号体系，收藏/心愿单先保持本地存储，账号体系后置。

### 6.3 开发阶段路线（按序执行）

**Phase 1 · 薄后端读接口（起点，让原型转真）**
- Node 后端，包 `pipeline/lib/derive.mjs` 为服务层，读 `plans.json`（后续换数据库，存储层形状不变）
- 接口（形状=mock.js）：`GET /api/routes/:routeId`（路线对+方案列表）、`GET /api/cities`、`GET /api/templates`、`GET /api/item/:id`
- 前端 `api.js` 桩切 fetch，签名不变，页面零改动
- 完成标准：七页在真后端下行为与读 mock.js 完全一致

**Phase 2 · LLM 直连三触点**
- 自然语言搜索解析（「国庆从杭州去喀什」→ 回填起终点/日期）
- 方案 AI 文案生成：LLM 输出走占位符模板，**必须过 validate-ai-copy 才展示**
- 站内问答（基于库内方案的 grounded QA）
- LLM key 只存服务端，浏览器不直连（防泄漏/控费/CORS）

**Phase 3 · 心愿单异步任务**
- 心愿登记 → 服务端任务队列 → agent 工具链执行（LLM 编排 + 数据采集工具）→ 三重守门写回 → 前端轮询/推送状态
- 前端现有 pending/generated 状态机不变

**Phase 4 · 数据源正规化（长期）**
- 聚合商 API 谈判，或策展库模式（只覆盖背包客高频线，离线扩库）

### 6.4 资产转正映射

| 现有资产 | 开发中角色 | 处置 |
|---|---|---|
| `pipeline/lib/derive.mjs` | 后端服务层核心 | 直接移植，勿重写 |
| `pipeline/lib/validate-*.mjs` | 写入路径校验中间件 | 直接移植 |
| `plans.json` + 方案库Schema.md | 数据库 schema + 种子数据 | 换 DB 时保持字段形状 |
| `mock.js` | API 响应契约 | 开发期保留作前端离线兜底 |
| `api.js` 桩 | fetch 实现的签名基准 | 换实现不改签名 |
| `app.js` localStorage 工具 | 收藏/心愿单本地存储 | 保留至账号体系上线 |
| 七页原型 | UI 蓝图 | 逐页转组件/模板 |
| `colors_and_type.css` | 设计令牌 | 主题基准 |
| pipeline 心愿脚本 | 异步 job 定义参考 | 重写为队列任务 |

## 7. 工程约定

- **技术栈**：前端保持多页渐进增强（零构建原则，SPA 化需用户确认）；后端 Node + **原生 Koa 或 Express**（见 §8 教训）。

- 交付物形态：MVP 产品文档 + 开发中的前后端代码 + `mixtouring-hifi/` 原型（UI 蓝图与离线兜底）+ 历史原型 `prototype/`。

- `mixtouring-hifi/` 冻结为蓝图：UI 微调可直接进行；结构级改动需在 `工作记录.md` 留版本记录。

- `pipeline/data/plans.json` 为存储层唯一数据源，`mock.js` 由 `build-mock.mjs` 生成**勿手改**。

- 开发代码目录建议新建 `server/` 与 `web/`（或按用户指定），不混入原型目录。

## 8. 经验教训

- **Koa 相关**：`koa-connect` 包装会导致 ctx 泄漏；如需 Koa 中间件复用，用原生 Koa 重写，不要包装 Express 中间件。

- 数据层 `store()` 读取时不要把默认值误传成参数，否则会先清空 localStorage（历史曾导致收藏/回填数据丢失）。

- **瞬时页面用 `location.replace`**：loading 等过渡页勿留在返回栈（历史曾导致「返回」回不到搜索页）。

- **数据源冲突是常态**（同一班次多来源多版本）：以「最新鲜来源优先 + sampled_at 时间戳 + source 字段」兜底，勿静默取其一。

- **淡季混搭未必省钱**（实测 9 月成都→乌鲁木齐直飞 ¥989 起，混搭仅省 ¥177）：如实展示，AI 文案转向体验价值，勿美化数字。
