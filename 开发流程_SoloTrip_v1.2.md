# MixTouring v1.2 开发流程｜从单程方案库到 Solo Trip 国际往返

**制定日期：2026-09-16｜最近更新：2026-09-19｜依据：`产品方案_SoloTrip_v1.2.md`｜状态：G1 规则底座与三项行程 API、G2 统一入口确定性路由与只读探索结果页（trip.html）已交付待复审；`下一卡＝G2 收口复审 → G2.5 AI 任意地点规划 → G3 多玩一城`**

> **G2.5 首卡范围（评审二轮确定，不混 G3）**：PlaceResolver 扩展（机场/车站/景点 + 别名）、CandidateBuilder 三策略骨架（直达优先/一次中转/混合交通，均为待验证假设）、逐段 searchWeb 取证、约束 chips（预算/最长中转时长/夜到到达/换乘次数）。G3 的城市停留完整旅程不提前混入。

本计划以北京 ⇄ 阿拉木图、2026 年国庆附近出发并返回作为第一条国际往返验证路径。今天离国庆约两周：能立即做的是采集真实日期样本、确认供给与交互；不能把完整国际往返产品、社区和稳定实时比价承诺为节前上线。2026 年样本应带查询时间封存，节后作为回放用例；后续还需选未来日期重新验证现价能力。

## 1. 当前资产、目标与优先级

| 当前仓库资产 | 可复用部分 | 当前缺口／下一步 |
|---|---|---|
| `mixtouring-hifi/pages/search.html` + `api.js` + `/api/parse` | 起终点及一句话入口、规则和 LLM 解析兜底 | 解析去返窗口与独旅约束；两字段直接进入结果；日期逐步补全 |
| `results.html` / `detail.html` | 策略卡、分段详情、风险展示 | 单程与历史价主导；重做往返两向时间线、证据状态、一人总成本与次级停留 |
| `pipeline/data/plans.json` + `方案库Schema.md` | 结构化分段、来源／样本时间、写入守门 | 无独立返程、国际时区、指定日期供给和完整费用；新增行程契约，旧库仅保留历史参考 |
| `derive.mjs` / `geo-skill.mjs` | 旧路线派生与地理候选筛选 | 新的 dated trip 规则不能修改旧数据为假往返；地理候选需与真实班期分离 |
| `server/index.mjs` + `verify.mjs` | API、静态站、降级和现有接口检查 | 新增行程级查询／修改／证据契约；验证去返、时区、过期状态和同口径比较 |
| `library.html` / `share.html` / `me.html` | 私人收藏与分享页面基础 | 私人行程版本、确认复盘和三个社区自愿入口需独立权限与运营流程 |

**实施纪律**：先做北京—阿拉木图这一条 OD 的真实双向证据闭环，再扩 3–5 条高价值 OD；保留 `/api/plans` 和 `api.js` 旧签名以免现有七页失效，新增行程服务逐步接管新体验。任何新写入复用结构校验、文案数字守门与合并后全库校验；`mock.js` 由管道生成，禁止手改。每个阶段提交前执行仓库三件套，并为新增契约增加针对性测试。

## 2. 开发阶段与交付门槛

以下工时是单人专注开发的初估，不包含外部数据源授权等待、用户招募和社区运营；按每周 10–15 小时推进时，用工时估算排期，不能把日期作为保证。

| 阶段 | 估算 | 交付物 | 进入下一阶段的门槛 |
|---|---:|---|---|
| G0 产品和证据取样 | 12–20 h，立即开始 | 本规格、北京—阿拉木图样本矩阵、原平台双向查询记录、供给源可用性结论 | 至少核实一个可比较的双向直达基准；每项来源、日期、币种、人数和有效期明确；若取不到，确定只发探索版 |
| G1 行程数据与规则底座 | 25–40 h | `TripQuery/TripDraft/Strategy/Leg/Evidence` 契约、版本和时区计算、去返独立校验、总成本与未知项、过期规则 | 国际往返不能由 B–A 反转；跨时区与跨日计算、缺段降级、同口径省钱判定通过自动测试 |
| G2 双入口与交通结果 | 25–40 h | 极简搜索／一句话解析、逐步补日期、结果状态、交通策略卡、直达占优诚实结论 | 两字段可直达首个探索结论；指定日期有证据时可比较；无证据时不出可执行卡；移动与桌面可用 |
| G2.5 AI 任意地点规划（v0.29.3 插入） | 15–25 h | PlaceResolver 扩展（机场/车站/景点+别名）、CandidateBuilder 三策略骨架（直达/一次中转/混合交通，纯交通类）、逐段 searchWeb 取证、约束 chips | 骨架一律 candidate_hypothesis（不含未经取证数字）；取证只走可用 searchWeb；未获证据段保持探索态 |
| G3 详情、停留、AI 修改 | 30–50 h | 去返时间线、费用和独旅提醒、次级城市拓展、重新校验后的版本、分段核验清单 | 停留 1／2 天后半程与返程仍完整；每次修改重算；原案可恢复；LLM 离线降级不污染事实 |
| G4 私人行程与封闭用户测试 | 20–35 h | 默认私密保存、版本与复盘草稿、12 位目标用户任务测试、问题修复 | 用户能独自完成逐段核验；数据质量无严重误导；保留真实备选指标可记录 |
| G5 社区三个选择试点 | 单独评估 | 路线复盘、城市交流／活动、临时同行独立入口及运营后台 | 只有在试点城市有真实内容／用户密度，且审核、举报、隐私处理和双向同意流程可运行时开放 |

G0–G4 约 112–185 小时，按每周 10–15 小时约 8–19 周；实际取决于交通数据源和返程证据。G5 不捆绑 P0 发布。2026 年国庆 case 的节前价值是**真实世界取样与可行性测试**，不是以两周实现全部阶段。

### G0：先采证据再排路线

1. 建立一个可回放的测试输入：起点北京、终点阿拉木图、1 人；去程 `2026-09-27～10-03`，返程 `2026-10-05～10-11`。日期范围是测试假设，受国庆及调休影响，用户可修改。选择至少 3 个去返日期组合：假期核心、错峰提前／延后、受 10 月 10 日上班影响的受限组合。
2. 每个日期组合分别查去程与返程直达；再探索一条有真实服务证据的转机候选和一条可能的铁路／航班混搭候选。若官方或可授权供应源无法证明某段，记录为未验证，不能放入 `dated_verified`。城市停留选项只在候选段成立之后取样。
3. 每次采样保存查询时间、原页面／接口链接、原始币种、旅客数、行李口径、退改条件、税费、当地起落日期时间和服务号；可保存脱敏截图／结构化快照用于回放，但不要把脆弱的动态网页引用伪装成稳定 API。
4. 确认官方入境规则链接与实际护照类型依赖；团队不能从“北京出发”推断国籍。国际数据涉及境外交通与支付／汇率，先评估合法可用的数据源和更新成本，再决定是否承诺指定日期可执行。

**G0 决策输出**：`可核验往返`、`仅部分核验` 或 `只有探索` 三选一，并给每个候选缺失字段。若直达占优，不强行造低价混搭；把顺路停留作为额外体验测试。

### G1–G2.5–G3：推荐的技术顺序（v0.29.3 更新：G2 收口后插入 G2.5）

1. **契约先行**：新增行程对象与验证器；定义 `direction`、本地时间／UTC 时间点、证据状态及过期规则。写确定性样例测试，再接真实样本。旧 `plans.json` 不在没有证据时补返程或固定某年国庆价。
2. **候选与比较**：检索已经存在或可采到的双向服务，筛掉班期／衔接不成立的组合；费用拆分与未知项计算在规则层。AI 只读取候选和证据，解释“为什么这条适合你”。
3. **API 与前端**：新接口返回事实、状态和下一步缺什么；前端先实现两字段与状态结果，然后详情、停留和修改。每次修改的输入／输出可回放，避免只是换一段文案。
4. **保存与核验**：行程私密版本、分段平台查询清单和回放 fixture 先于公开分享。所有链接用于用户复查，不宣称本产品掌握余票或出票能力。

### v0.25.4 后的下一批工作卡与放行顺序

1. **G1 底座收口**：以 `validateStrategy`、`buildCostBreakdown`、`evidenceState` 的同一输入构建服务层响应；分段来源／班次、有效取样、去返时序、已知及未知的交通／接驳／独住费用必须贯穿校验、状态与展示。补明确的结构化证据和费用契约，禁止只改卡片标签而仍引用旧总价。`check-trip` 固定时钟回放与旧三件套全绿，逐项复核当前 G0 fixture 仍未达到 `dated_verified`。
2. **G2 第一张卡：只读行程查询和核验清单**。先为北京—阿拉木图实现 `searchTripStrategies`／`getVerificationChecklist` 的新服务动作，返回 `query`、各方向分段、证据态、来源与采样时间、未知费用和缺口清单；仅使用库内已审查证据，旧 `/api/plans` 和 `api.js` 签名继续兼容。无日期或缺真实指定日来源时仅返回探索／历史参考，不生成可执行卡，不把旧国内单程反转为国际返程。新增接口测试覆盖 T03、T05–T08、T11，空库和来源失效必须有诚实结果。
3. **G2 第二张卡：两字段入口与交通结果**。两字段直接进入结果，随后可补去返日期；一句话解析提取可编辑的意图，不推断国籍。首屏先展示直达、转机、铁路／混搭的已知结构与证据级别，明确“尚需逐段查询”；直达有优势时直说。移动与桌面走 T01、T02、T04 流程，不能因界面出现具体月份而误示当日询价已完成。
4. **G2.5 先行（AI 任意地点规划）**：PlaceResolver 扩展 + CandidateBuilder 三策略骨架（直达/一次中转/混合交通，纯交通类）+ 逐段 searchWeb 取证 + 约束 chips；骨架一律为 candidate_hypothesis（待验证假设，不含未经取证的具体数字）
5. **G3 随后实施详情、修改和停留**：每次约束修改都重新运行规则并产生可回看版本；停留 1／2 晚时须补齐后半程、返程及单人住宿和接驳成本。缺任一关键证据时保留探索/部分核验状态，不显示确定省额。完成 T09、T10、T12 的回放和移动／桌面验收后再进入 G4 私人保存与用户测试；G5 社区试点单独判定。

每张卡按“输入 fixture → 失败/成功断言 → 服务动作 → 前端呈现 → CI 与人工抽查 → 工作记录”交付。G0 的 2026 国庆网页线索只作回放和核验清单；获得原平台同日期、同人数、同行李的去返证据，并核清场站、班次、税费及有效期前，不把任何一张样本升级为指定日期可执行或“更省”卡。

**v0.26.1 交接说明**：G1 服务动作已完成契约统一：解析输出 `YYYY-MM-DD ~ YYYY-MM-DD` 日期窗；检索按 `trip_type` 选方向，并在每段保留来源、取样时间、有效日期及双时区；核验清单 API 以 `{query, strategy_id}` 从服务端样本重取策略，拒绝客户端自报分段。下一张卡仍为 G2 只读行程查询与核验清单前端；先展示证据级别和缺口，页面不得把 G0 样本升为可执行报价。

## 3. 北京—阿拉木图测试用例及验收规则

测试输入为国庆附近北京 ⇄ 阿拉木图；执行测试时用当期真实采样，并把取样日、来源和原始输出固化为 fixture。下面“预期”描述系统行为，不指定未经核验的实际航班、票价或省钱结果。

| ID | 操作／条件 | 预期 |
|---|---|---|
| T01 | 只输入“北京”“阿拉木图” | 直接进入交通结果；未补日期时标探索／历史参考，不假称国庆真实报价 |
| T02 | 输入“国庆附近一个人去阿拉木图再回北京” | 可编辑地提取 OD、往返、1 人和日期意图；不从北京出发推断护照国籍 |
| T03 | 选择去返日期窗口 | 去返方向分别取证；任一返程缺失时整卡只为部分核验，不能标完整往返 |
| T04 | 有直达基准且其他组合总成本更高或时间风险更大 | AI 首屏说直达更合适；不显示虚假省钱标签；城市停留仍是可选模块 |
| T05 | 某混搭票面价低，但独住住宿、接驳或行李费用未获证据 | 展示已知拆账和“待核验费用”；不能将未知当 0 或给确定节省额 |
| T06 | 有两段去程与一段返程证据，返程实际为另一个班次 | 去返详情分别显示原始服务及来源；不由去程反转生成返程 |
| T07 | 当地起落时间跨午夜、北京与阿拉木图时区不同 | 展示两地当地日期和时区；UTC 时点计算飞行／换乘间隔，跨日不出现负时长 |
| T08 | 中转跨机场／车站或需自行提取行李 | 明确单人接驳时间、行李与衔接风险；不足最短连接时间的候选不成为可执行卡 |
| T09 | 选“多玩一座城市”并停 2 晚 | 交通结果仍在前；生成含后半程、两晚独住费用和返程的扩展版本；标明增加多少已知时间／成本 |
| T10 | 修改为“不要凌晨到站，最多换一次” | 结构化条件重排并重新校验；没有可行方案时如实说明，不保留违反条件的旧卡 |
| T11 | 样本过期、链接不可用或 LLM key 缺失 | 过期降级为参考、给重新查询动作；LLM 缺失时规则／事实结果仍可工作 |
| T12 | 保存行程、产生修改版本、打开核验清单 | 默认私密；旧版可回看；清单逐段列日期、场站、服务和原来源，未获证据项目另列 |
| T13 | 用户自愿公开路线复盘 | AI 草稿须用户编辑确认；个人证件、住宿地址、实时精确位置不在公开内容；客观变更不自动写入事实库 |
| T14 | 试点用户只想交流城市活动，不想找同行 | 交流、同行开关独立；可保留私人行程并退出交流；没有开放试点时入口不制造活跃假象 |

**自动化覆盖**：G1 起对 T03、T05–T08、T10–T12 编写服务层或 E2E 检查；用显式伪造 fixture 检查“不出卡／不省钱／不反转”，这是防误导的必要回归。G2–G3 对 T01、T02、T04、T09 做移动与桌面端流程验证。T13–T14 属 G4／G5 的权限和运营验收，不能提前用只有 UI 的“功能完成”替代。

## 4. 质量门禁与工作流程

每张开发卡遵循：**问题与用户触发 → 证据／契约 → 规则和 UI → 针对性验证 → 文档／工作记录 → 提交**。先 `git fetch origin` 并阅读工作记录最新版本；若多人改同一逻辑，基于最新 main 做小而完整的提交。每个产品变更按 `v0.x.y + 日期 + 背景／改动／影响` 倒序写入 `工作记录.md`。

提交前必跑 `node pipeline/check-pages.mjs`、`node pipeline/check-geo.mjs`，启动服务后跑 `node server/verify.mjs`。新增行程规则时增加日期、时区、返程方向、价格同口径和证据状态测试；保留旧接口回归。CI 对旧三件套及新规则覆盖均绿才合并。真实来源采样由可追溯的事实登记表记录；人工抽检只抽当前日期可核验卡，需核对去返原站／机场、班期、服务号、费用和链接。

### 4.1 产品评审 → ZCode 修复 → 复审

固定交接入口为 [产品评审记录 Issue #5](https://github.com/lucashuang-an/MixTouring/issues/5)（v0.26.2 修订：评审由用户在本对话主动发起，不使用定时任务/自动工作流/API 唤醒）。用户转述 ZCode 的提交、PR、变更或测试结果；Codex 基于实际 diff、测试和本方案的阶段门槛作只读评审。每轮评审在本 Issue 发布**被审 SHA、结论（通过／有条件通过／需修复／阻断）、按优先级排序的修复项及可复现依据、逐项验收标准、下一开发卡目标与能力边界**，并在用户对话同步摘要。无新增缺陷时也需写明「无新增修复项」和已验收的范围；证据不足不写「通过」。

ZCode 每次开工前读取该 Issue 最新的带 SHA 评审。若为`需修复`或`阻断`，先修并在同一 Issue 回复**修复项编号、对应提交 SHA、实际运行的测试及残留问题**；Codex 对新 SHA 复审并更新结论。若通过，再按最新评审给出的下一卡执行。Issue 评论用于交接与追踪，`工作记录.md` 继续记录产品／实现版本事实；两者均不得把 CI 成功、历史票价或路线探索写成指定日期可执行旅行。公开 Issue 不包含凭据、个人数据及安全敏感复现细节。

发布前至少完成：1）北京—阿拉木图双向样例的所有 `dated_verified` 卡逐段复核；2）无证据／过期／直达占优三类诚实结果；3）移动与桌面无障碍操作；4）12 位 solo trip 用户完成真实任务并记录实际保留的备选。若 G0 缺稳定取样源，先发布明确标注的路线探索与核验助手，暂不开放指定日期“省钱”比较。社区三个入口是愿景，但开放运营由 G5 单独放行。

## 5. 待决事项与取舍准则

| 待决问题 | 判定方式 | 若未解决 |
|---|---|---|
| 是否有合法、稳定的国际航班／铁路供给与可用价格源？ | G0 比较来源覆盖、费用、时效和授权条件 | 降级为探索和清单，不能称实时比价 |
| 首批独旅者究竟更看重费用、时间、可预期衔接或城市停留？ | 12 位用户同任务访谈，对照其平台检索与方案保留 | 先不固定“最省钱”作为推荐主轴 |
| 城市停留是否真的提高选择价值？ | 看结果后自愿打开、修改和实际核验后的保留率 | 保持次级，不挤占交通主任务 |
| 三类社区体验何时有足够密度与运营能力？ | 1–2 城小范围内容质量、交流效果、双向同意和处理时效 | 仅保存私人计划与可编辑复盘，不开放即时同行 |

产品基线以 `产品方案_SoloTrip_v1.2.md` 为准；工时和测试矩阵以本文件为准；旧 `开发计划.md` 为历史路线。后续每个 gate 完成后，在工作记录写清真实结果、证据缺口、下一阶段范围和版本，不把计划项写成已上线功能。
