# MIXTOURING · MVP 产品文档

> ⚠️ **本文件为历史版本（v0.3，2026-08-30）**。产品定位已于 v0.18.0 重写，定位、硬约束、指标与交互以 `产品定义v1.1.md` 为准。
> 本文件保留作为演进留痕（§4.2/§5 的在线实时询价描述已过期）。


> 版本 v0.3 · 2026-08-30 · 基于产品方案 v0.2.2（第 2 轮决策②后）
> 配套交付：原型图 `prototype/mvp-prototype.html`（界面编号 S1–S7 与本文 §4 一一对应）
> 前期文档：《市场调研 × 初步产品方案》（`budget-route-app-research/`）、《工作记录.md》

## 1. 需求背景

2025 年国庆，创始人从北京前往喀什，直飞机票与平台推荐的中转方案都超出预算，最终靠数小时人工检索拼出「北京 →（火车）银川 →（飞机）阿克苏 →（火车）喀什」的混合路线，省下约一半费用。市场调研佐证了这不是个案 [调研佐证]：

- 中转/联程玩法比直飞便宜 30%–50%；飞猪回旋镖机票单航段均价 356 元，同期整体 640 元，低 44%。
- 国内没有产品支持「自选中转城市 × 跨交通方式 × 日期窗口」的自由组合比价；官方空铁联运仅覆盖 40+ 城市。
- 目标人群（泛背包客，约 18–35 岁）时间弹性大、价格极度敏感，且乐于研究并分享路线——既是工具的用户，也是社区的种子。

两轮共创已确定产品方向（《工作记录.md》v0.2–v0.2.2）：定位为「背包客的 AI 省钱路线规划师 + 路线分享社区」，纯内容与方案产品，不做交易；Web 先行；AI 走 LLM 混合架构；社区为「模板库 × 体验笔记」双轨。本文件把这些决策收敛为可开发的 MVP 需求。

## 2. MVP 目标与验收

MVP 验证一个假设：AI 生成的省钱中转方案，质量足以让真实背包客采纳。

- 北极星指标：方案可用率——生成方案中「分段真实存在、衔接可执行」的比例。
- 配套指标：社区实测回流率——出行后完成实测回填的用户比例。

验收用例（第 1 轮共创已定，双验收）：

1. 输入「北京 → 喀什、国庆日期窗、预算优先」，系统自动复现创始人人工路线，或给出总花费不高于其 10% 的等价方案。
2. 10–20 名种子背包客盲测：方案可用率 ≥ 80%，采纳意愿 ≥ 50%。

## 3. MVP 范围

| 包含 | 不包含（及原因） |
|---|---|
| M1 AI 路线引擎（搜索输入 + 方案生成） | 买票、出票、跳转下单（创始人决策：不做交易） |
| M2 方案卡片 | 用户账号体系（第 2 轮待决策 Q4；MVP 按「匿名 + 本地保存」实现，标注 TBD） |
| M3 风险透明 | M6 预算反查（P2） |
| M4 路线库（模板列表 / 详情 / 复用） | M7 体验笔记（P2，社区双轨之内容层） |
| M5 分享链接 + 实测回填 | 低价监控、学生认证（第 1 轮已降级/后置） |
| | App / 小程序（Web 先行） |

## 4. 功能需求详述

### 4.1 搜索与约束输入（S1）

**页面布局**：首页即搜索页。顶部为品牌区与一句话定位；中部为搜索表单（出发地、目的地、日期窗口、预算上限、偏好）；底部为例行入口（热门远途 OD 快捷 chips + 001 号路线展示位）。

**业务逻辑**：出发地与目的地必填，城市粒度，支持车站/机场别名归一（如「北京西」「PEK」归一到北京）。日期窗口 = 出发日 ± 浮动天数。预算上限选填。偏好三选一，决定方案列表的排序规则。提交后跳转方案列表页并进入「生成中」状态。出发地与目的地相同、日期窗浮动超过 7 天等非法输入在表单内即时拦截并给出提示。

**字段规则**：

| 字段 | 类型 | 必填 | 约束 | 默认 |
|---|---|---|---|---|
| 出发地 | 城市 | 是 | 国内城市，别名归一 | 无 |
| 目的地 | 城市 | 是 | 同上，且不等于出发地 | 无 |
| 日期窗口 | 日期 + 浮动天数 | 是 | 今天起 365 天内，浮动 0–7 天 | 浮动 ±3 天 |
| 预算上限 | 数字 | 否 | 100–99999 元 | 不限 |
| 偏好 | 单选 | 是 | 预算优先 / 耗时优先 / 均衡 | 预算优先 |

### 4.2 AI 方案生成（S2 生成中状态背后的引擎流水线）

**业务逻辑**（第 2 轮决策①定稿的四步流水线）：

1. 候选预筛：AI 基于「地理中间性 + 交通枢纽库」选出候选中转城市集合，收敛候选空间、抑制幻觉。
2. 逐段询价：对候选组合的每个分段，通过 OTA/第三方询价 API 取价（数据接入，非交易）；铁路段用 12306 公布票价规则推算；询价结果按 OD + 日期窗缓存。
3. 方案生成：AI 综合价格 / 耗时 / 中转城市可玩性，产出 1 个直飞基准 + 2–5 个中转方案（空空中转、空铁、铁铁、夕发朝至）。
4. 数据校验：方案卡上每个数字（价格、时刻、耗时）必须出自数据层查询结果，无数据不出卡；校验失败的方案整条丢弃。

生成时长预期 10–30 秒，超过 60 秒进入失败态并给出重试入口。某一分段询价失败时，对应方案降级为「价格待补」并显著标注，不允许用模型推测填数。

### 4.3 方案卡片列表（S2）

**页面布局**：顶部为搜索条件摘要条（可点回修改）；其下为直飞基准卡（作为对比锚点）；主体为方案卡流。每张卡包含：分段路线示意、总花费估算、总耗时、「比直飞省 X 元 / 多花 Y 小时」、风险分徽标、中转城市可玩性星级。

**业务逻辑**：卡片价格以「近期估算区间」呈现并标注样本时间，与可购票价切割（不做交易决策的落地）。列表顶部固定展示估算语义说明。排序随偏好即时切换：预算优先按总价升序，耗时优先按总耗时升序，均衡按综合分。点击卡片进入方案详情（S3）。空结果时给出「放宽日期窗 / 调整预算」的引导，而非空白页。

**边界与异常**：部分分段询价失败 → 对应卡片标注「价格待补」并置底；全部失败 → 失败态 + 重试按钮；生成中超 60 秒 → 超时失败态。

### 4.4 方案详情与风险透明（S3）

**页面布局**：顶部为方案总览（卡片信息 + 完整路线示意图）；中部为分段明细（每段：交通方式、车次/航班号、发抵时刻、历时、估算价格、班次密度提示）；下部为风险透明区（M3）与中转城市可玩性卡；底部为操作区（复制分享链接、本地收藏、实测回填入口）。

**业务逻辑**：风险分由四个结构化因子合成，每个因子单独展示原始值与判级理由，不输出黑箱总分：

- 衔接时间余量：小于 1 小时为红，1–2 小时为黄，大于 2 小时为绿。
- 行李直挂：跨承运人需自提行李为黄。
- 退改规则严苛度：不可退改为黄，分段各自展示。
- 机场-车站接驳距离：大于 30 公里为黄。

可玩性卡展示中转城市「停留 4–24 小时可玩内容」摘要（景点密度、物价水平、通行提示），数据来自内容库；无数据时整个模块不展示，不显示占位文案。

### 4.5 路线库（S4 列表 / S5 详情）

**页面布局**：列表页为模板卡流，每张卡含路线名（如「北京→喀什省钱路线」）、分段示意、近期实测均价、实测次数、复用按钮；顶部为官方精调标识与方向筛选（西部 / 东北 / 海南）。详情页为模板的完整方案详情（结构同 S3）+ 实测记录列表（每条：用户标识、出行日期、实际花费、衔接顺利度、一句话评价）。

**业务逻辑**：模板来自官方精调的 30–50 条「直飞贵」热门远途 OD，001 号路线为创始人的北京→喀什路线。「一键复用」把模板的 OD 与偏好预填进搜索表单（S1），按用户自己的日期窗重新生成方案——模板不锁定历史价格，只复用路线结构。实测记录按时间倒序展示，其中的价格与衔接数据回流引擎，用于样本库滚动校准。

### 4.6 分享（S6 落地页）

**业务逻辑**：方案详情（S3）与模板详情（S5）均可生成分享链接，链接即传播单元（Web 先行的天然优势，适配小红书/微信传播）。被分享者打开落地页看到只读版方案卡 + 路线示意 + 「按同样条件搜我的」按钮（跳转 S1 并预填 OD）。分享页不含任何购买入口，底部为产品一句话介绍与回首页入口。

### 4.7 实测回填（S7）

**业务逻辑**：出行完成后，用户从方案详情或分享页进入回填表单。提交后数据进入样本库：实际花费用于票价校准，衔接顺利度用于风险分因子校准。回填是 MVP 的社区最小闭环动作（分享 → 他人复用 → 实测回流）。匿名状态下回填记录与浏览器本地标识关联，待账号体系决策后支持迁移（TBD）。

**字段规则**：

| 字段 | 类型 | 必填 | 约束 | 默认 |
|---|---|---|---|---|
| 实际总花费 | 数字 | 是 | 1–99999 元 | 无 |
| 实际出行日期 | 日期 | 是 | 不晚于今天 | 无 |
| 衔接顺利度 | 单选 | 是 | 顺利 / 紧张但赶上 / 误衔接 | 无 |
| 分段实付明细 | 数字 × N | 否 | 与原方案分段一一对应 | 无 |
| 一句话评价 | 文本 | 否 | 不超过 140 字 | 无 |

**状态流转**（回填单）：

| 当前状态 | 允许操作 | 下一状态 | 触发条件 |
|---|---|---|---|
| 待回填 | 提交表单 | 已回填 | 用户提交 |
| 已回填 | 系统处理 | 已入库校准 | 数据层每日校准任务 |

## 5. AI 引擎专项

**准确率定义**：方案可用 = 全部分段在数据层可查（车次/航班真实存在且当日有班），且同站衔接 ≥ 45 分钟、跨站跨场衔接 ≥ 90 分钟。可用率按生成方案逐条统计。

**评估体系**：上线前以验收用例①（北京→喀什复现）做回归；盲测期按用例②口径统计；上线后每周用实测回填中的反例（误衔接、票价偏差大于 20%）回溯流水线各环节。

**兜底与降级**：询价 API 不可用时以票价样本库兜底，并标注「历史样本」；模型服务超时时返回已通过校验的方案子集（可能少于 2 个）并明确提示；任何情况下不允许模型输出未经数据层校验的数字。

**成本约束**：询价结果按 OD + 日期窗缓存，机票缓存 6 小时、铁路缓存 24 小时 [PM 判断，可调]；同一 OD 的重复搜索命中缓存时直接复用。

## 6. 数据与合规注意

- 询价 API 仅作数据接入，产品不持有交易资质、不引导下单；API 选型（航旅纵横/飞常准等开放平台 vs OTA 开放平台）待技术验证后定。
- 铁路票价为 12306 公布定价规则的推算值 + 社区实测校准，界面统一标注「估算」语义。
- 路线库内容均为官方精调或用户授权分享，不抓取第三方攻略全文，规避版权风险。
