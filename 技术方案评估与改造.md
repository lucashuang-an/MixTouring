# MIXTOURING · 技术方案评估与改造

> 基于 v0.17 代码实跑（本地起服务、走完整旅程、跑作者自带三件套、外部核验数据）。
> 配套文档：《MixTouring-产品定义-v1.0.md》

---

## 第一部分 · 现状评估

### 1. 实跑事实

本地 `node server/index.mjs` 启动正常，装载 25 城 / 8 路线对 / 15 方案 / 6 模板。
作者自带的 `verify.mjs` / `check-pages.mjs` / `check-geo.mjs` **全部通过**。

心愿队列真实状态（8 条）：

| 状态 | 数量 | 备注 |
|---|---|---|
| generated | 3 | **全部由人工执行器完成** |
| pending | 5 | 其中 3 条 attempts≥1 后失败或转人工 |

**自动采集器（v0.15–v0.17，跨两个模型、并行检索、两阶段组装、地理护栏）真实产出可用方案：0 条。**

### 2. 七个不足（按严重度排序）

#### 🔴 P0-1 价格基准系统性偏高，「省钱」结论方向可能是反的

| 路线 | 产品基准 | 同一 source 字段里自记的真实低价 | 方案宣称 |
|---|---|---|---|
| 成都-三亚 | ¥950（"非促销典型值"） | 携程促销 ¥330 | 省 ¥171 |
| 北京-西双版纳 | ¥1,681 | 携程促销 ¥570 | 省 ¥567 |
| 北京-喀什 | ¥2,480（manual_sample，无说明） | 外部可查单程约 ¥1,130 | 省 ¥1,295 |

北京→喀什的 ¥2,480 约等于外部查到的**往返**价，也约等于单程低价的两倍。按真实单程重算，p-bjks-1（¥1,115–1,255、23h17m、两次中转）**相对直飞几乎不省钱，只多花 17 小时**。

**根因**：三重守门体系防的是「LLM 编造不存在的数字」，防不住「真实采样数被放在错误的比较位置」。省额全部来自数据层字段、完全合规，但是错的。

#### 🔴 P0-2 日期参数完全无效

`/api/plans?from=北京&to=喀什&date=2020/01/01` 返回与国庆完全相同的方案和价格。日期在整条链路上没有被消费。MVP §4.1 定义的「日期窗口 ±3 天」从未实现，且在当前数据源下**不可能实现**。

#### 🔴 P0-3 旗舰路线没有车次号

存储层检查结果：

```
p-bjks-1/2/3, p-bjwl-1/2, p-shkm-1/2  → segs 中无 train_no / flight_no 字段
p-cdwl-*, p-njks-*, p-hzks-*, p-cdsy-*, p-bjxbn-*  → 有
```

即：**创始人的 001 号北京→喀什路线，是唯一无法按图索骥的那条**。用户看到「21:30 从北京西出发」，得自己去猜是哪趟车。违反 MVP §4.4 的明文要求。

#### 🟠 P1-4 路线单向存储，反向查询返回空

`喀什→北京` 返回 `{direct:null, plans:[]}`，而界面文案写的是「直飞基准 · 北京 **⇄** 喀什」。返程用户会以为没有数据。

#### 🟠 P1-5 问答规则版答非所问

实测三问：

| 提问 | 规则版回答 |
|---|---|
| 北京去喀什哪个方案最省钱 | 按价排序的三方案 ✅ |
| 北京到喀什的火车方案**舒服吗** | **一模一样**的价格清单 |
| 十一去喀什**要带什么** | 北京→喀什 + 南京→喀什 两组价格清单 |

v0.13.1 修的是「跨路线混淆」，没解决意图识别。这是所有 key 失效/超时/额度耗尽的兜底路径。

#### 🟠 P1-6 LLM 做票价采集的路线已被自己证伪

工作记录里的失败模式高度一致：直飞价格/时长幻觉、时刻算术必错、模型不会稳定自触发联网搜索、摘要不足以组装。v0.16 换 glm-5.3-flash + 独立 web_search API 之后仍是 0 产出。

**这不是调参问题，是把最不可靠的组件放在了最需要确定性的工位上。**

#### 🟡 P2-7 测试体系只验证内部一致性

三件套断言的全部内容：接口返回是否与 `buildServiceDB` 派生结果逐字节一致、mock.js 与 API 是否一致、地理判定是否符合自定规则。

**没有一条断言在验证外部真实性**——这个车次今天存在吗、这个价格买得到吗、这个方案真的比用户自己订划算吗。所以出现了「三件套全绿 + 用户旅程断裂」的局面。

### 3. 需要保留的资产（不要推翻）

| 资产 | 评价 |
|---|---|
| `geo-skill.mjs`（绕行比 + 顺路投影窗口） | **最有价值的代码**。24 城判定与人工策展一致。是新架构的候选剪枝层，直接复用 |
| `derive.mjs`（存储→服务派生、四因子判级） | 判级规则代码化质量高，迁移到新模型时保留规则，改输入 |
| 三重守门 / 占位符插值 / 禁手写数字 | 防幻觉机制本身正确，需要扩展到「基准选取」维度 |
| 七页前端 + 零构建渐进增强 | 成本低、可维护，继续用 |
| 一句话解析（parse.mjs） | 实跑表现最好的 AI 触点，规则兜底不编造。保留并加强 |
| 版本留痕纪律 | 项目最强的工程习惯，继续 |

---

## 第二部分 · 目标架构

### 4. 四层分工

```
        ┌──────────────────────────────────────────────┐
        │ L5 用户侧 AI：M8 目的地反查 · M9 对话式回填     │  在线，LLM，在主链路两端
        └───────┬──────────────────────────┬───────────┘
                │ 产出 O/D                  │ 产出边校验信号
                ▼                          │
┌─────────────────────────────────────────────────────┐
│ L4 表达层：方案卡 / 价格区间叙述 / 跳转链接 / 风险文案      │  规则 + 模板
├─────────────────────────────────────────────────────┤
│ L3 搜索层：图搜索 + 地理剪枝 + 可行性过滤 + 帕累托前沿      │  确定性算法（在线，无 LLM）
├─────────────────────────────────────────────────────┤
│ L2 边库：Edge 存储 + 时效管理 + 校验状态                  │◄──┘
├─────────────────────────────────────────────────────┤
│ L1 造边层：铁路规则计算 / 航班时刻整理 / LLM 抽非结构化供给  │  离线，LLM 在这里
└─────────────────────────────────────────────────────┘
```

**AI 的工位从 L3（想方案）挪到 L1（造边）和 L5（理解用户 / 整理反馈）。** L3 中间那段保持确定性，这是整个改造的核心，其他都是它的推论。

注意 L5 → L2 那条回边：M9 收上来的不是评论，是**边的真伪信号**，直接回流到护城河。这是社区与数据资产第一次被焊在一起。

### 5. 数据源分级

| 数据 | 有无 API | 个人可得 | 方案 | 准确度上限 |
|---|---|---|---|---|
| 铁路车次时刻 | 无官方 API | ✅ 公开、准静态（一年两次调图） | 时刻表整理 | 100% |
| 铁路票价 | 无 | ✅ 公布定价规则 | **按里程×席别计算** | 100% |
| 航班时刻/班期/航司 | 无免费 API | ✅ 公开、准静态 | 时刻表整理 | ~98% |
| 航班实时报价/余票 | 有，需资质 | ❌ | **不做**，跳转自查 | — |
| 航班历史价格区间 | 无 | ⚠️ 公开页面可见 | 低频采样，仅作区间参考 | 粗粒度 |
| 大巴/班线/接驳 | 无 | ✅ 公告、官网、攻略 | **LLM 抽取** | 需校验 |

**关键结论**：把「实时报价」从依赖链里摘掉之后，**其余全部数据都是个人可得的慢数据**，且铁路段可以做到 100% 准确。

### 6. Edge Schema

```jsonc
{
  "edge_id": "rail-Z69-BJP-YNJ",
  "mode": "rail | air | bus | ferry | transfer",
  "carrier": "中国铁路 | GJ | ...",
  "vehicle_no": "Z69",                    // 必填，无车次号的边不入库
  "from": { "city": "北京", "station": "北京西", "code": "BXP" },
  "to":   { "city": "银川", "station": "银川",   "code": "YIJ" },
  "dep": "21:30",
  "arr": "08:12",
  "arr_day_offset": 1,
  "duration_min": 642,
  "frequency": { "type": "daily | weekly | seasonal", "days": [1,2,3,4,5,6,7] },
  "season": { "from": "2026-03-29", "to": "2026-10-24" },   // 航季/调图周期
  "price": {
    "kind": "fixed | band | unknown",
    "fixed": { "硬卧": 263, "硬座": 152 },   // 铁路：公布票价，可信
    "band":  { "min": 780, "max": 920, "sampled_at": "...", "n": 6 },  // 机票：仅供参考
    "deeplink_tpl": "https://..."          // 跳转模板，用户自查
  },
  "source": { "type": "rule_calc | timetable | llm_extract", "url": "...", "note": "..." },
  "verified_at": "2026-09-11",
  "confidence": 0.95
}
```

**接驳边（transfer）单列**：机场↔火车站、市内跨站，带距离、方式、耗时。这是四因子风险判级的输入，也是 LLM 抽取的重点对象。

### 7. 图搜索层设计

```
输入：O, D, 月份, 偏好
① 剪枝：geo-skill.candidatesBetween(O, D) → 候选中转城 8–12 个      ← 复用现有代码
② 建子图：只取「O ∪ 候选 ∪ D」之间的边                              ← 规模可控
③ 路径枚举：DFS，深度 ≤3 段（≤2 次中转）
④ 可行性过滤：
     同站换乘 ≥45min
     跨站换乘 ≥90min 且 ≥（接驳耗时 + 30min 缓冲）
     班期对得上（周班次、航季）
⑤ 帕累托前沿：(总耗时, 价格特征, 风险分) 三维取非被支配解
⑥ 截断：最多 5 条，按偏好排序（省钱/最快/最好玩）
```

**复杂度**：候选 12 城、每对城市平均 5 条边，子图约 300–700 条边，路径枚举在毫秒级。**不需要任何在线 LLM 调用，不需要任何在线 API 调用。**

**「创造 OTA 里不存在的方案」在这里变成确定性的**：只要边在图里，组合必然被枚举到，不会漏、不会编、可解释。

### 8. 铁路票价计算（最值得先做的模块）

铁路是唯一能做到 100% 准确的部分，且往往占混搭成本一半以上。

```
票价 = f(里程, 席别, 车型系数) + 附加费
```

普速（K/T/Z）按公布的基准费率 × 里程 × 席别系数；动车组（D/G）按公布的席别票价表。这些规则是公开的，实现一次可以长期使用，只在铁总调价时更新。

**建议第一个 sprint 就把它做完**——它能立刻把「敢打包票的数字」从 0 提升到覆盖一半以上的分段，是重建信任的支点。

### 9. LLM 造边管线（AI 的真正工位）

```
目标供给（大巴/班线/接驳/新开航线）
        ↓
① 检索：确定性查询词（"银川河东机场 中卫 机场大巴 时刻"）
        ↓
② 抽取：LLM 从页面正文抽成 Edge 结构 —— 只做抽取，不做推理、不做算术
        ↓
③ 校验（不可绕过，复用现有守门思路并扩展）：
     - 结构完整：vehicle_no / 站点 / 时刻 缺一即弃
     - 算术归码：duration 由代码从时刻推导，**不接受 LLM 给的时长**
     - 地理合理性：速度带校验（公路 40–90km/h，铁路 60–300km/h，航空 400–900km/h）
     - 来源必须有 URL，无来源不入库
     - 交叉验证：同一条边需 ≥2 个独立来源，或标 confidence 低并在前端弱化
        ↓
④ 入库 + verified_at 打戳
        ↓
⑤ 时效管理：超 90 天自动进复核队列
```

**与现有采集器的关键差别**：

| | 现在（采集方案） | 改造后（采集边） |
|---|---|---|
| LLM 任务 | 组装一整条含价格的方案 | 抽取一条边的结构化事实 |
| 失败代价 | 整条方案作废（已发生 5+ 次） | 一条边作废，不影响其他 |
| 算术 | LLM 做（已证明必错） | 代码做 |
| 价格 | LLM 采（幻觉高发） | 铁路算 / 机票不采 |
| 复用性 | 一条方案服务一个 OD | 一条边服务所有经过它的 OD |
| 时效压力 | 小时级（价格） | 月级（时刻） |

**这是把 LLM 从「必输的任务」调到「必赢的任务」上。** 从非结构化文本抽结构化事实，是 LLM 最擅长的事，也正是你产品里唯一没人能替你做的事。

### 10. 价格表达引擎

无实时报价条件下的价格叙述生成器（纯规则，无 LLM）：

```
输入：直飞采样区间 + 各段价格特征
输出：
  - 铁路段：精确值 + "公布票价"标注
  - 机票段：不出数字，出跳转
  - 直飞参考：区间 + 采样时间 + 采样口径
  - 关系叙述：模板化生成
      if 直飞区间跨度 > 2 倍：
        "直飞报价波动大（¥{min}–{max}）。高于 ¥{threshold} 时此组合通常更划算，
         促销期直飞更优。"
```

**并新增一条守门规则**：任何输出文案中出现「省 ¥数字」模式即拒绝写盘——把 §4 的产品铁律变成不可绕过的代码约束，与现有防幻觉体系同构。

### 11. 跳转链接构造

每段生成 deeplink，把用户带到平台对应查询页（**不是下单页**，交易边界不变）：

- 铁路 → 12306 查询页（车次 + 日期）
- 机票 → 携程/天巡的 OD + 日期查询页

这既解决了价格准确性，也解决了合规（我们不展示他人价格数据，只做导流），还补上了断掉的「最后一公里」——顺便让「采纳」变成可埋点事件，支撑新的北极星指标。

### 12. 测试体系改造

现有三件套保留（内部一致性仍需保证），**新增外部真实性断言层**：

| 新增检查 | 方法 | 频率 |
|---|---|---|
| 车次真实性抽检 | 抽 20 条边核对官方时刻表 | 每次发版 |
| 边库时效 | verified_at 超 90 天占比 | 每日 CI |
| 价格文案守门 | 正则扫「省 ¥\d+」模式 | 每次写盘 |
| 换乘可行性 | 有效余量为负的方案数必须为 0 | 每日 CI |
| 双向对称 | 每条 OD 的反向查询非空 | 每次发版 |

### 13. L5 用户侧 AI 的技术要点（M8 / M9）

完整设计见《用户侧AI功能设计-M8-M9.md》，这里只列对架构有影响的部分。

#### 13.1 新增数据文件

`pipeline/data/destinations.json` —— 城市画像库，M8 召回的依据。

```jsonc
{
  "city": "喀什",
  "themes": ["边境","古城","民族风情","美食"],
  "traits": { "altitude_m": 1290, "crowd_level": "low",
              "physical_demand": "low", "best_months": [4,5,6,9,10], "min_days": 4 },
  "reachable": { "has_direct_air": true, "in_edge_graph": true, "typical_routes": 3 }
}
```

与已有的 `cities-geo.json`（179 城地理事实）是**互补关系**：geo 库回答"能不能到、顺不顺路"，destinations 库回答"想不想去"。建议共用城市主键，分文件维护。

#### 13.2 LLM 调用的四个 kind

现有 `callJson({kind})` 机制直接扩展，不改适配层：

| kind | 用途 | 模型档位建议 | 降级 |
|---|---|---|---|
| `parse` | 一句话行程解析（已有） | 低 | 规则引擎 |
| `intent` | M8 偏好解析 | 低 | 关键词映射 |
| `pick` | M8 候选择优 + 写理由 | 中（约束遵循要求高） | 匹配分排序 + 模板句 |
| `recall` | M9 抽取 + 笔记草稿 | 中 | 退回原表单 |

`ask`（问答）保留，定位改为「随行助手」，context 从全库方案列表收窄为**单条方案的结构事实**——这同时解决了实跑发现的 P1-5（问答答非所问）：问题范围窄了，规则兜底才写得有意义。

#### 13.3 守门复用（零新增校验器）

| 风险 | 复用的现有机制 |
|---|---|
| M8 提名候选外的城市 | 白名单校验，与 `parse.mjs` 的 24 城白名单同构 |
| M8/M9 文案手写数字 | **`validate-ai-copy.mjs` 一行不用改**（占位符白名单 + 去占位符后禁阿拉伯数字） |
| M9 抽出方案里不存在的车次 | 与该 plan 的 segs 做交集校验，不在集合内的信号丢弃 |
| M9 抽取算术（耗时、偏差） | 代码从时刻推导，不接受 LLM 给的数值——与造边管线同一条铁律 |

**这是现有防幻觉体系第一次产生复利**：为离线管线建的守门，用户侧直接可用。

#### 13.4 M9 → 边库的回流接口

```
POST /api/recall            { plan_id, raw_text }
  → { structured, note_draft }                    // 待用户确认，不入库

POST /api/recall/confirm    { plan_id, structured_edited }
  → 写入 feedback 样本库
  → 遍历 structured.edge_signals，写入边库复核队列：
       confirmed  → edge.verified_at 刷新，confidence 上调
       changed    → edge.status = needs_review，前端该方案加"信息可能已变"
       not_found  → edge.status = suspect，**立即从图搜索中排除**
       接驳偏差    → transfer_edge.duration_min 进加权平均，连续 3 次触发复核
```

`suspect` 状态要接进图搜索的过滤条件——**这是用户反馈第一次能直接影响产品输出**，也是边库自净的机制。

#### 13.5 成本与时延

| 调用 | 预估 token | 频次 | 时延要求 |
|---|---|---|---|
| `intent` | ~400 in / ~150 out | 每次反查 1 次 | <2s |
| `pick` | ~1200 in（候选列表）/ ~300 out | 每次反查 1 次 | <4s |
| `recall` | ~800 in / ~500 out（含笔记） | 每次回填 1 次 | <8s（可加载动画） |

三者都是**低频、可等待、错了能重来**的调用——与被证伪的造边采集（高频、阻塞、错了整条作废）在风险结构上完全相反。这也是我认为这两个功能值得做的技术理由。

---

## 第三部分 · 迁移路径

### Phase 0 · 前提验证（1 周，先于一切开发）

选 30 条长线，人工查真实最低直飞价 vs 可能的混搭组合，统计「混搭确有优势」的比例。

- ≥60% → 按下述 Phase 1–3 执行
- ≈10% → 停止引擎投入，产品收敛为深度内容型，AI 用在内容与问答

**这一步不写代码，但它决定后面所有代码是否值得写。**

### Phase 1 · 止血（1–2 周）

不改架构，先让现有产品不说假话：

1. 全库省额下线，改为价格区间叙述（§10）
2. 补齐 7 条老方案的车次/航班号（§P0-3）
3. 日期控件改为「出行月份」，或直接移除（§P0-2）
4. 风险卡标题改显示**有效余量**（§S3）
5. 每段加跳转链接
6. 停掉自动采集器的进一步投入（0 产出，见 §P1-6）

**做完这六项，产品就从「可能误导用户」变成「有限但诚实」**，这比任何新功能都重要。

### Phase 2 · 建边库（4–6 周）

1. Edge schema 落地，`plans.json` 的 15 条方案**拆解成边**（存量不浪费）
2. 铁路票价规则计算模块（§8）
3. 主要城市铁路/航班时刻表整理（先覆盖现有 25 城）
4. 图搜索层（§7），接 geo-skill 做剪枝
5. `derive.mjs` 的四因子判级迁移到新输入

**完成标志**：北京→喀什的原始路线被**算出来**而不是被存储，且三段车次正确。

### Phase 2.5 · 用户侧 AI（2–3 周，可与 Phase 2 并行）

1. **M9 对话式回填**先做——不依赖边库完工，现有 15 条方案上就能跑；`recall` 调用 + 确认卡 + 复用 `validate-ai-copy` 守门，是整个改造里**投入产出比最高的一项**
2. **M8 目的地反查**后做——依赖 `destinations.json` 城市画像库和边库的 `in_edge_graph` 字段，建议在 Phase 2 图搜索可用后接入
3. 问答（M6）挪到详情页，context 收窄为单条方案

**为什么 M9 可以先于边库**：它的价值一半在数据（要边库）、一半在内容与回流率（不要边库）。先跑起来，等边库就位后回流通道自然接上。

### Phase 3 · 造边自动化（持续）

1. LLM 造边管线（§9）+ 校验层
2. 优先抽取：机场↔火车站接驳、城际大巴、新开航线
3. 时效复核队列 + CI 定时任务（现有 `collect.yml` 改造复用）
4. **接 M9 的复核队列**——用户信号和自动采集共用同一条校验通道

**这个阶段开始，边库规模成为产品的核心增长指标**，也是 AI 真正开始产生复利的地方。

---

## 附：一句话总结

现在的架构是「**LLM 想方案 + LLM 查价格 + 代码做校验**」，其中 LLM 承担了两件它最不擅长的事。

目标架构是「**代码想方案 + 不查价格 + LLM 造供给 + LLM 懂用户**」，让每个组件都做自己必赢的任务：

- 答案唯一、错了代价高的事（时刻、票价、换乘）→ 代码
- 输入模糊、长尾无穷、错了代价低的事（想去哪、怎么玩、说了什么）→ AI
- 没有 API、藏在非结构化文本里的事（班车、接驳、新航线）→ AI，且只有 AI

你的原始洞察——让 AI 创造 OTA 里不存在的交通方案——在新架构里不但没有被削弱，反而第一次变成了可规模化、可验证、可积累的机制：**你的图里有多少条别人没有的边，就是你的产品有多少条别人给不出的方案。**
