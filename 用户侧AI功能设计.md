# MIXTOURING · 用户侧 AI 功能设计（M8 / M9）

> 配套：《产品定义 v1.1》《技术方案评估与改造》
> 范围：目的地反查（M8）、对话式实测回填（M9）。两者是 v1.1 唯一的两个面向用户的 AI 入口。

---

## 0. 设计原则（两个功能共用）

| 原则 | 含义 |
|---|---|
| **AI 不产出结构性数字** | 车次、时刻、票价、换乘余量、距离，一律由代码从数据层插值。AI 可以说"时间比较紧"，不能说"还有 43 分钟" |
| **AI 只做三件事** | 理解模糊输入 / 从非结构化文本抽结构 / 生成叙述。其余全是代码的事 |
| **输出必须落在白名单上** | M8 只能推荐边库覆盖内的城市；M9 只能抽出 schema 内的字段。越界即丢弃 |
| **降级要少而准** | key 失效时给"更少但正确"的结果，绝不给"更多但可能错"的结果 |
| **每个结论可追溯** | 推荐要能点开看理由；抽取要能让用户确认修改 |

---

# M8 · 目的地反查

## 1. 它解决什么

现在的产品要求用户先想好 O 和 D。但背包客真实的起点常常是：

> 「国庆七天，从北京出发，三千以内，想去有雪山、人少、不用爬太多的地方」

这是 OTA **结构上无法接受的输入**——它们的搜索框必须填目的地。

三个战略价值：

1. **低频 → 中频**：查路线一年 1–3 次，"想出去玩，去哪好"一个月冒好几次。
2. **把覆盖短板变成推荐理由**：推荐时优先给边库已走通的目的地。用户不会觉得"你怎么没有"，只会觉得"你推荐的这条正好有走法"。
3. **内容可传播**："从北京出发三千块能去的五个偏远目的地"在小红书能传播，"北京到喀什的中转方案"不能。

## 2. 用户流程

```
S0 反查入口（搜索页下方一行 / 独立 tab）
   「还没想好去哪？说说你想要什么样的旅行」
        ↓
用户输入一段自然语言（也可从预置 chips 起步：雪山 / 人少 / 海边 / 古城 / 边境）
        ↓
① 解析：LLM 抽出结构化偏好（不做推荐）
        ↓
② 补全：缺关键项时**只追问一个**（出发地或预算或天数，按缺失优先级）
        ↓
③ 候选召回：代码从边库 + 城市画像库筛出候选集（20–40 个）   ← 这一步没有 LLM
        ↓
④ 排序与择优：LLM 从候选集中选 3–5 个并写推荐理由           ← 只能从候选里选
        ↓
⑤ 结果卡：目的地 + 一句理由 + 可达性标签 + 「看怎么去」
        ↓
点「看怎么去」→ 预填 O/D 跳 S2 结果页 → 接回主链路
```

**关键设计**：③ 是代码，④ 是 LLM。**LLM 永远不自由提名目的地，只在代码给出的候选集里做选择和叙述。** 这和 geo-skill 给 LLM 注入候选中转城是同一个模式——你已经验证过它有效。

## 3. 两次 LLM 调用的结构

### 调用一：偏好解析（kind=`intent`）

```
system:
你是旅行偏好解析器。从用户的话里抽取结构化偏好，只输出 JSON，不做推荐。
不确定的字段一律 null，禁止猜测。

输出 schema:
{
  "origin": "城市名或 null",
  "budget_total": 数字或 null,          // 单位元，整段旅行交通预算
  "days": 数字或 null,
  "month": "YYYY-MM 或 null",
  "themes": ["雪山","人少","海边","古城","边境","草原","沙漠","徒步","美食","民族风情"],  // 白名单内多选
  "avoid": ["高海拔","长途硬座","爬山","热"],    // 白名单内多选
  "companions": "solo|couple|friends|family|null",
  "flexible_date": true|false|null
}

themes / avoid 必须取自上述白名单，白名单外的表达映射到最接近项，无法映射则丢弃。
```

### 调用二：候选择优（kind=`pick`）

```
system:
你是旅行推荐官。从【候选列表】中选出最匹配用户偏好的 3–5 个，并为每个写一句推荐理由。

铁律：
1. 只能从候选列表中选，禁止提名列表外的城市。
2. 理由中禁止出现任何数字（价格、时长、海拔、距离）——这些由系统插值。
3. 理由不超过 40 字，说"为什么适合这个人"，不是介绍景点。
4. 若候选中无一匹配，返回空数组，禁止硬凑。

输出: { "picks": [ {"city":"...", "reason":"...", "theme_hit":["..."]} ] }

【用户偏好】{解析结果}
【候选列表】{代码召回的 20–40 个城市，每个带：主题标签、可达性、典型交通时长档、是否已走通}
```

**为什么理由里禁止数字**：这是把现有的防幻觉铁律（占位符插值 + 禁手写数字）直接套到用户侧。理由文案里写"只要八百多"是幻觉高发点，而它恰恰是最影响信任的地方。

## 4. 候选召回（代码层，无 LLM）

需要一个新的数据文件：`pipeline/data/destinations.json`

```jsonc
{
  "city": "喀什",
  "themes": ["边境", "古城", "民族风情", "美食"],
  "traits": {
    "altitude_m": 1290,
    "crowd_level": "low",          // low/mid/high，人少程度
    "physical_demand": "low",      // 是否需要体力
    "best_months": [4,5,6,9,10],
    "min_days": 4                  // 建议最少停留
  },
  "reachable": {
    "has_direct_air": true,
    "in_edge_graph": true,          // ★ 边库是否已走通
    "typical_routes": 3
  }
}
```

**召回规则（纯代码）**：

```
1. 主题匹配：themes ∩ 用户 themes 非空
2. 排除：avoid 命中即剔除（如 avoid=高海拔 → altitude_m > 3000 剔除）
3. 月份：month ∈ best_months（软条件，不满足降权不剔除）
4. 天数：min_days ≤ 用户 days
5. 可达性：从用户 origin 出发，边库中存在路径 → 强加权
6. 预算：用边库算出的该 OD 最低组合价格特征做粗筛（铁路段精确 + 机票段区间下沿）
7. 排序取前 20–40 送给 LLM
```

**第 5 条是关键**：已走通的目的地强加权。这样 LLM 选出来的，大概率是点进去就有结果的。

## 5. 结果卡

```
┌──────────────────────────────────────┐
│ 喀什 · 新疆                           │
│ 边境古城，秋天人少，不用爬山             │  ← LLM 写的理由，无数字
│                                      │
│ 🚄✈️ 已走通 3 条路线 · 最快约 10 小时    │  ← 代码插值
│ 铁路段合计 ¥335 起 · 机票段需自查        │  ← 代码插值
│ 建议 4 天以上 · 9–10 月最佳             │  ← 代码插值
│                                      │
│           [ 看怎么去 ]                │
└──────────────────────────────────────┘
```

**边界情况**：

| 情况 | 处理 |
|---|---|
| 候选为空 | 「按你的条件我们还没走通合适的线」+ 给 3 个最接近的已走通目的地 + 引导放宽 |
| 目的地未走通但匹配度高 | 卡上标「这条线还没走通」+ 「登记心愿」按钮，**不隐藏**——诚实也是内容 |
| LLM 返回列表外城市 | 整条丢弃，落规则版（按匹配分排序取前 3，理由用模板句） |
| LLM 理由含数字 | 该条理由替换为模板句，其余保留 |

## 6. 降级（无 key / 超时）

规则版完全可用：主题标签匹配 + 可达性加权排序，理由用模板拼装（"边境 · 古城 · 秋季适宜"）。

**降级标准**：给 3 个而不是 5 个，理由从一句话变成标签串。**少而准。**

---

# M9 · 对话式实测回填

## 1. 它解决什么

现状：回填是一个表单，匿名 + localStorage，换设备清零，现实回流率 <2%。而且目标错了——用它校准票价，数学上不可能收敛。

改造后**一举三得**：

| 收益 | 机制 |
|---|---|
| 摩擦降低 | 从填表变成说一段话 |
| 边库校验 | 抽结构信息（车次属实、接驳耗时、换乘顺利度），**小样本就有效** |
| 内容资产 | 同时生成体验笔记草稿，笔记层冷启动的供稿来源 |

**关键认知**：把一件"用户帮你的杂活"变成"你送用户的礼物"。用户交出一段话，拿回一篇能直接发小红书的笔记。

## 2. 用户流程

```
触发：方案详情页 / 分享页 / 「我的」页，出行日期已过 → 「这趟你走了吗？说两句」
        ↓
用户输入一段话（文本，后续可加语音转写）
        ↓
① 抽取：LLM 从这段话抽出结构化反馈 + 边校验信号
        ↓
② 确认：把抽取结果以**可编辑卡片**呈现，用户点几下改正（不是让他重填）
        ↓
③ 入库：反馈进样本库，边校验信号进边库复核队列
        ↓
④ 回礼：同一次调用生成一篇笔记草稿，用户可编辑 / 复制 / 生成分享图
```

## 3. LLM 调用设计（kind=`recall`）

**一次调用，两份输出**（省 token，且两者共用同一段上下文）：

```
system:
你是旅行实测记录的整理员。用户刚走完一条混合交通路线，会说一段话。
你要做两件事：
(A) 把话里的事实抽成结构化 JSON
(B) 写一篇 150–250 字的体验笔记草稿

铁律：
1. 只抽用户明确说了的，没说的一律 null，禁止推断、禁止补全。
2. 笔记草稿中禁止出现任何用户没说过的数字。方案里的车次/时刻/价格由系统插值，
   你在需要处写占位符 {seg1_no} {seg1_price} {transfer_city}，不要自己写数字。
3. 用户语气保留，不要改写成营销腔。

输出:
{
  "structured": {
    "smooth": "顺利|紧张但赶上|误衔接|null",
    "actual_cost": 数字或 null,              // 用户明确说了金额才填
    "transfer_feedback": [
      { "city":"银川", "issue":"接驳耗时超预期|班次不符|站点变更|无", "detail":"..." }
    ],
    "edge_signals": [                          // ★ 边库校验信号
      { "vehicle_no":"Z69", "status":"confirmed|changed|not_found", "detail":"..." }
    ],
    "play_feedback": { "city":"银川", "sentiment":"positive|neutral|negative", "mentions":["怀远夜市"] },
    "would_recommend": true|false|null
  },
  "note_draft": "……{seg1_no}……"
}

【本方案结构事实】{该 plan 的分段、车次、中转城、换乘余量}
【用户原话】{...}
```

**为什么笔记草稿也用占位符**：完全复用你已有的 `validate-ai-copy.mjs` 守门（占位符白名单 + 去占位符后禁阿拉伯数字）。这个校验器一行不用改就能直接用在用户侧，是现有资产的复用。

## 4. 确认卡交互

抽取完不直接入库，先给用户一张**可编辑卡**：

```
┌────────────────────────────────────────┐
│ 我记下来了，看看对不对：                    │
│                                        │
│ 换乘  [顺利] [紧张但赶上✓] [误衔接]        │  ← 单选，可改
│ 银川接驳  比预计慢  ✓  （大巴堵车）          │  ← 可删
│ Z69 车次属实  ✓                          │  ← 可删
│ 怀远夜市 好评  ✓                          │  ← 可删
│ 实际花费  [ 未填写 ]                      │  ← 可补
│                                        │
│              [ 确认 ]                   │
└────────────────────────────────────────┘

确认后 ↓

┌────────────────────────────────────────┐
│ 顺手给你写了篇笔记，改改就能发：             │
│ ┌────────────────────────────────────┐ │
│ │ 北京到喀什，我没坐直飞……（可编辑）      │ │
│ └────────────────────────────────────┘ │
│   [ 复制 ]  [ 生成分享图 ]  [ 发到路线库 ] │
└────────────────────────────────────────┘
```

**为什么要确认步骤**：抽取必然有错。让用户点两下改正，比让他重填一遍成本低一个量级，而且**确认动作本身让数据质量有了人的背书**——这些数据要进边库，不能是纯 LLM 输出。

## 5. 边校验信号的处理（这是 M9 最重要的产出）

用户说的每一条与边有关的事实，进边库复核队列：

| 信号 | 动作 |
|---|---|
| `confirmed` | 该 Edge 的 `verified_at` 刷新，`confidence` 上调 |
| `changed`（时刻变了/站点变了） | Edge 标 `needs_review`，前端该方案加"信息可能已变"提示，进人工/LLM 复核队列 |
| `not_found`（车次不存在了） | Edge 标 `suspect`，**立即从图搜索中排除**，等复核 |
| 接驳耗时偏差 | 该 transfer edge 的 `duration_min` 进加权平均；偏差连续 ≥3 次触发复核 |

**这就是为什么 M9 比旧回填有价值**：旧回填收的是价格（需要海量样本才收敛），新回填收的是**结构真伪**（一个用户说"这趟车没了"就足够触发复核）。目标对了，小样本机制就成立。

## 6. 降级

无 key 时：退回原表单（衔接顺利度三选一 + 花费 + 一句话评价）。**但入口文案要变**——「说两句」变成「填两项」，不要让用户以为是同一个东西然后失望。

## 7. 隐私与身份

匿名 + localStorage 仍是 MVP 默认（账号体系 Q4 未决）。但笔记发到路线库时需要一个署名，建议：

- 发布时生成一个**旅行者代号**（如「银川转机的老王」），存本地 + 随笔记入库
- 明确告知「换设备后这个代号找不回来」
- 这既保留了匿名，又给了身份感，是账号体系落地前的过渡

---

## 附：两个功能的指标

| 功能 | 主指标 | 护栏 |
|---|---|---|
| M8 目的地反查 | 反查 → 点「看怎么去」转化率 | 推荐目的地中"已走通"占比 ≥70%；列表外提名率 = 0 |
| M9 对话式回填 | 回填完成率（对比旧表单基线） | 抽取字段用户修正率 <30%；笔记草稿采用率 |

**M9 的隐藏指标更重要**：每月产生的有效边校验信号数。这是社区真正反哺护城河的度量。
