# MIXTOURING · 产品定义 v1.1

> 本版基于对 v0.17 代码与数据的实跑体验结论重写。
> 与 v0.3《MVP产品文档》的关系：**定位层重写，交互层大部分保留**。
> 核心变化：从「省钱比价引擎」改为「混合交通路线发现器」——产品对**结构**负责，不对**实时价格**负责。
>
> **v1.1 相对 v1.0**：新增两个面向用户的 AI 入口——M8 目的地反查、M9 对话式实测回填。
> 详细设计见《用户侧AI功能设计-M8-M9.md》，本文档只收录定位、模块、交互与指标层面的结论。

---

## 0. 为什么要重写

v0.3 定义的产品，承重墙是一个精确数字：「比直飞省 ¥X」。实跑之后这堵墙塌了：

- 直飞基准取的是「非促销典型值」（成都-三亚 ¥950，而自己 source 里记着携程促销 ¥330；北京-喀什 ¥2,480，而外部可查单程约 ¥1,130）。省额是拿一个人工挑选的高基准减一个采样值得来的，**方向可能是反的**。
- 日期参数被完全忽略（传 2020/01/01 返回同样的价格），因为底层数据源本就无法按日期出价。
- 没有商家资质 → 拿不到有授权的实时报价接口 → 这堵墙**在可预见的未来都立不起来**。

但把价格拿掉之后，最初的洞察仍然完整地活着：

> 「北京→银川→阿克苏→喀什 这个组合可行」——这句话不含价格，不会过期，不需要实时数据，
> 而 OTA 因为按 OD 切分交易，结构上永远不会告诉你。

**v1.0 把产品建在这句话上。**

---

## 1. 定位

**MixTouring 是混合交通路线的发现器：告诉你从 A 到 B，除了直飞，还存在哪些跨交通方式的组合走法。**

| | v0.3 | v1.0 |
|---|---|---|
| 卖点 | 比直飞省 ¥X | 存在这样一条走法，OTA 不会告诉你 |
| 承重数据 | 实时票价 | 时刻表 + 换乘可行性 + 地理 |
| 数据时效 | 小时级（做不到） | 月级（做得到） |
| 准确性上限 | 由 OTA 数据决定 | 由自己的边库质量决定 |
| 价格角色 | 核心结论 | 参考区间 + 跳转自查 |
| 可防御性 | 无（携程随时能做） | 边库（携程不会去做） |

**一句话定位**：背包客的混合交通路线发现器——找出直飞之外的火车 × 飞机 × 大巴组合，把每一段的车次和换乘讲清楚，价格由你自己去平台查。

### 1.1 硬约束（违反即返工）

沿用旧版三条，新增两条：

1. **不做交易**：无购买入口、不出票、不持有交易资质。（保留）
2. **无数据不出卡**：结构信息缺失的方案整条丢弃。（保留）
3. **风险因子不给黑箱总分**：每个因子单独展示原始值与判级理由。（保留）
4. **【新】不输出精确省额**：任何形如「省 ¥1,295」的表述一律禁止。价格只能以区间关系表达（见 §4）。
5. **【新】每个分段必须可执行**：车次号/航班号 + 发抵站点 + 时刻，三者缺一不出卡。没有车次号的方案没有价值。
6. **【新】用户侧 AI 不产出结构性数字**：AI 面向用户的输出（推荐理由、笔记草稿、问答）中，车次、时刻、票价、时长、距离一律走占位符插值。AI 可以说"时间比较紧"，不能说"还有 43 分钟"。复用现有 `validate-ai-copy.mjs` 守门，未通过即整条丢弃落规则版。

### 1.2 明确不做

| 不做 | 原因 |
|---|---|
| 实时报价、余票查询 | 无资质、无授权接口、做不准 |
| 日期窗口比价 | 依赖上一条，一并后置 |
| 出票、跳转下单分成 | 交易边界（v0.2 决策，保留） |
| 全量 UGC 内容流 | 冷启动期精力不够，笔记层走策展制 |
| 全国任意 OD 覆盖 | 边库覆盖到哪里，产品就到哪里，如实声明 |

---

## 2. 核心资产：从「方案库」到「边库」

这是 v1.0 最重要的结构变化。

**旧模型**：资产 = `plans.json` 里 15 条写死的完整方案。新增一条路线 = 人工或 LLM 生成一整条方案 → 三重守门 → 写回。产出慢、不可复用、一条线只服务一个 OD。

**新模型**：资产 = **边（Edge）库**。一条边 = 一个具体的可乘交通服务。

```
边(Edge) = { 交通方式, 车次/航班号, 出发站, 到达站, 发时, 到时, 班期, 价格特征, 来源, 校验时间 }
```

方案不再被存储，而是被**算出来**：给定 O/D，在边库上做图搜索，实时拼出所有可行组合。

这个改变带来的差别：

| | 方案库模型 | 边库模型 |
|---|---|---|
| 新增一条北京→银川的火车 | 只服务它所在的那一条方案 | 服务所有经银川中转的 OD |
| 覆盖 N 个城市 | 需要人工产出 O(N²) 条方案 | 需要收集 O(N) 量级的边 |
| 新方案发现 | 靠人/LLM 想出来 | 算法自动穷举出来 |
| 「创造 OTA 里没有的方案」 | 靠 LLM 发挥（已证伪） | 图上必然出现（确定性） |

**「创造 OTA 里不存在的方案」在这个模型下不再是一个生成任务，而是图搜索的自然结果。** 你的原始洞察第一次变成了可规模化的机制。

### 2.1 边的三个来源（按可得性分级）

| 边类型 | 数据可得性 | 谁来做 |
|---|---|---|
| 铁路（车次、时刻、票价） | 公开、准静态、定价规则公布 | 规则计算，**准确率可达 100%** |
| 航班（航司、时刻、班期，**不含价格**） | 公开、准静态 | 时刻表整理 |
| 大巴 / 机场跨城巴士 / 地方班线 / 口岸班车 / 拼车 | **没有任何 API，散落在公告、攻略、官网 PDF 里** | **LLM 抽取 —— 这是 AI 不可替代的位置** |

第三类是护城河。携程不会去整理银川河东机场到中卫的班车时刻，但这条边一旦进了图，你就能算出它算不出的方案。

---

## 3. 用户与场景

**主用户**：泛背包客，18–35 岁，时间弹性大，价格敏感，目的地偏远。（保留 v0.2 决策）

**核心场景**：用户已经决定要去某个偏远目的地，发现直飞贵或没有直飞，想知道「还有没有别的走法」。

**关键认知修正**：这是一个**低频**需求（一年 1–3 次），且**供给稀疏**（混搭只在部分 OD 成立）。因此：

- 不做「工具入口」型产品（等用户来搜），做**内容型分发**（路线本身是内容，在小红书/SEO 被发现）。
- 覆盖率诚实声明是核心体验的一部分，不是失败态。「这条线我们还没走通」是合法且频繁的答案。

---

## 4. 价格表达规范（产品级规则，最高优先级）

没有实时报价能力的情况下，价格怎么说才既有用又诚实。

### 4.1 三条铁律

1. **禁止输出精确省额。** 不允许出现「省 ¥1,295」「便宜 ¥171」。
2. **禁止用单次采样作为基准。** 直飞基准必须是区间，且标注该区间的采样口径与时间。
3. **每个分段必须有跳转入口**，让用户查当日真实价格。价格的权威在平台，不在我们。

### 4.2 允许的表达形式

```
✅ 直飞参考：¥1,100–2,500（近 3 个月公开报价区间，采样于 2026-09）
✅ 本方案铁路段固定 ¥335（公布票价，准确）；机票段需自查
✅ 旺季直飞报价高于 ¥1,800 时，此类组合通常低 30–50%
✅ 淡季直飞常有促销（低至 ¥330 量级），此时直飞更划算
❌ 比直飞省 ¥1,295
❌ 总花费 ¥1,115–1,255
```

### 4.3 铁路段是例外

铁路定价固定，可以给**精确价格**，并标注「公布票价，非估算」。这是产品里唯一敢打包票的数字，要用足——它往往占混搭方案成本的一半以上，是建立信任的支点。

---

## 5. 功能模块

| 编号 | 模块 | 优先级 | 相对 v0.3 |
|---|---|---|---|
| M1 | 路线发现引擎（图搜索 + 地理剪枝 + 帕累托排序） | P0 | 重写（原 AI 生成） |
| M2 | 方案卡（结构 + 车次 + 价格区间 + 跳转） | P0 | 重写（原含省额） |
| M3 | 风险透明（四因子） | P0 | 保留，修标题 |
| M4 | 边库与造边管线（LLM 抽取 + 校验） | P0 | 新增（核心资产） |
| **M8** | **目的地反查（AI）** | **P0** | **新增**，产品的第二入口 |
| **M9** | **对话式实测回填（AI）** | **P0** | **新增**，替代 M5 的表单回填 |
| M5 | 路线库 / 分享 | P1 | 保留；回填部分并入 M9 |
| M6 | 站内问答 | P1 | 保留，定位改「随行助手」，挂详情页 |
| M7 | 体验笔记（策展制） | P2 | 保留；供稿来源改为 M9 产出的笔记草稿 |
| — | 日期窗口比价 | 冻结 | 依赖实时报价，不做 |
| — | 预算反查 | 冻结 | 同上 |

---

## 6. 核心逻辑

### 6.1 一次查询的流水线

```
用户输入 O / D / 出行月份（非具体日期）
        ↓
① 地理剪枝：geo-skill 给出候选中转城（绕行比 ≤1.6 且顺路投影 t∈[0.02,0.98]）
        ↓                                    ← 已有，保留，这是你的资产
② 图搜索：在边库上找 O→D 的所有可行路径（≤2 次中转）
        ↓                                    ← 新建，替代 LLM 生成
③ 可行性过滤：换乘时间是否够（同站 ≥45min，跨站 ≥90min + 实际接驳时长）
        ↓
④ 帕累托前沿：在（总耗时 × 价格特征 × 风险分）三维上取非被支配解，最多 5 条
        ↓
⑤ 表达层：套 §4 的价格规范，生成方案卡
        ↓
结果页（毫秒级，零 LLM 调用）
```

**关键点**：整条**路线查询**链路不调 LLM。响应是毫秒级、可解释、无幻觉的。这正是 v0.4.1「离线 AI 工厂 + 在线规则引擎」的决策，只是把「工厂」的产物从**方案**换成了**边**。

**M8/M9 不在这条链路上。** 它们是主链路的**前置入口**（S0 反查 → 得到 O/D → 才进入上述流水线）和**后置闭环**（走完之后回填），各自可以慢、可以等、可以出错重来。这是 AI 站在路线两端、代码守住中间的分工：

```
出发前（模糊）              中间（确定）                旅途后（长尾）
  S0 目的地反查      →   ①–⑤ 图搜索流水线      →      S7 对话式回填
      M8 · AI              代码 · 无 LLM                  M9 · AI
```

### 6.2 日期怎么处理

现在的日期控件是假的（传什么都返回同一结果）。两个选择：

- **推荐**：输入项改为「出行月份」+「是否节假日」。这两个变量真正影响的是价格区间的叙述（旺季/淡季）和班期（季节性航线），而这两件事我们**真的能回答**。
- 具体日期只在跳转链接里使用（把用户带到平台的对应日期查询页）。

**不要留一个看起来能比价、实际不比价的日历。** 这是期望欺骗，和 v0.13.1 修掉的「伪装实时规划的 loading 仪式」是同一类错误。

---

## 7. 用户体验与交互

界面编号沿用 S1–S7，新增 S0；改动集中在 S0/S1/S2/S3/S7。

### S0 · 目的地反查（M8，新增）

**入口**：搜索页搜索卡下方一行「还没想好去哪？说说你想要什么样的旅行」，或底部导航独立 tab。

**为什么它是第二入口而不是附属功能**：查路线是一年 1–3 次的低频动作，"想出去玩，去哪好"是一个月冒好几次的念头。同时它能把覆盖率的短板转成推荐的理由——优先推荐边库已走通的目的地，用户不会觉得"你怎么没有"，只会觉得"你推荐的这条正好有走法"。

**流程**：自然语言输入 → LLM 解析偏好（不推荐）→ **代码**从城市画像库 + 边库召回候选 20–40 个 → LLM 只在候选内择优 3–5 个并写理由 → 目的地卡 → 「看怎么去」预填 O/D 跳 S2，接回主链路。

**关键约束**：LLM 永远不自由提名目的地，只在代码给出的候选集里选择和叙述——与 geo-skill 给 LLM 注入候选中转城是同一个已验证的模式。推荐理由中禁止出现任何数字，数字全部由代码插值。

**未走通的目的地照常展示**，标「这条线还没走通」+ 登记心愿入口。诚实本身也是内容。

### S1 · 搜索页

**布局**：品牌区 → 一句话定位 → 搜索卡 → 覆盖说明 → 精选路线入口。

**变化**：

- 日期控件 → 「出行月份」下拉 + 「节假日出行」勾选。
- 保留一句话自然语言输入（实跑表现良好，是产品最成熟的 AI 触点之一）。
- 搜索卡下方**常驻一行覆盖声明**：「目前已走通 N 条路线、覆盖 M 个城市。没搜到不代表不存在，欢迎登记。」
  —— 把覆盖率的有限性前置，用户预期一开始就是对的，空态就不再是挫败。
- 偏好三选一改为：**最省钱 / 最快 / 最好玩**（「最好玩」按中转城可玩性排序，这是你的差异化，现在被埋在详情页里浪费了）。

### S2 · 结果页

**方案卡新结构**（从上到下）：

```
┌────────────────────────────────────────┐
│ 火车 + 飞机 + 火车 · 3 段 2 转 · 约 23 小时   │  ← 结构优先，不是价格优先
│ 北京 —🚄→ 银川 —✈️→ 阿克苏 —🚄→ 喀什        │
│                                        │
│ 铁路段 ¥335（公布票价）· 机票段需自查        │  ← 敢打包票的数字放前面
│ 直飞参考 ¥1,100–2,500（2026-09 采样区间）    │
│                                        │
│ ⚠️ 银川换乘有效余量 43 分钟 · 行李不直挂      │  ← 风险前置到卡上
│ 🏙️ 银川可玩                              │
└────────────────────────────────────────┘
```

**变化要点**：

1. **标题行是结构，不是省额。** 用户第一眼看到的是「有这样一条走法」。
2. **风险从详情页提到卡上。** 现在用户要点进去才知道换乘只剩 43 分钟，这是最该前置的信息。
3. **直飞基准卡保留**，但改为区间 + 采样时间 + 「查当日价」跳转。
4. 空结果时给三条**已走通的相邻路线**，而不只是「放宽条件」的空话。

### S3 · 方案详情

**分段明细必须包含车次号**（当前最大缺陷，001 路线完全没有）：

```
🚄 Z69  北京西 21:30 → 银川 08:12(次日)   10h42m   ¥263 硬卧（公布票价）
        [ 去 12306 查票 ]
        ↓ 换乘 银川站 → 河东机场 18km，机场巴士约 40 分钟
        ⚠️ 有效余量 43 分钟（原始间隔 1h53m − 接驳 40min − 缓冲 30min）
✈️ GJ8559 银川河东 10:05 → 阿克苏 13:40    3h35m    价格需自查
        [ 去携程查价 ]
```

**风险四因子文案修正**（实跑发现的矛盾）：

- 现在：标题「衔接时间余量 · 银川 **1h53m**」+ 等级「**高**」+ 理由「不足 1 小时」——三个数字互相打架。
- 改为：标题「衔接余量 · 银川 **有效 43 分钟**」，理由行展开算式。**标题显示有效余量，不是原始间隔。**

**新增「为什么值得走」区块**（替代原 AI 解读卡的省额叙事）：

> 直飞北京→喀什每周 38 班，旺季报价常在 ¥1,800 以上。这条线用夜车省下一晚住宿，
> 银川可停留半天。淡季直飞有促销时，这条线不划算。

这段话不含任何精确金额，但信息量比「省 ¥1,295」大得多，而且**永远是真的**。

### S4/S5 · 路线库

保留。「一键复用」只复用**路线结构**（中转城 + 交通方式），这个设计在新模型下更成立了——因为结构本来就是产品的资产。

### S6 · 分享页

保留（无购买入口红线已验证通过）。新增：分享卡片上突出「3 段 2 转 · 23 小时 · 经银川」的结构摘要，这是最有传播力的信息。

### S7 · 实测回填 → 改为对话式（M9）

**两处改变：目的变了，形式也变了。**

**目的**：不再指望它校准票价（匿名 + 极低回流率，数学上不可能收敛）。改为收集**结构真伪**——换乘是否顺利、车次是否还存在、接驳耗时是否符合预期。这类信号**一个用户说"这趟车没了"就足够触发复核**，小样本即有效。

**形式**：从填表改为「说一段话，AI 帮你整理」。

```
用户：银川转机差点没赶上，大巴堵了，飞机倒是准点。夜市不错，人均三十多。
        ↓
AI 抽出：换乘=紧张但赶上 / 银川接驳偏慢 / Z69 属实 / 怀远夜市好评
        ↓
可编辑确认卡（用户点两下改正，不是重填）
        ↓
① 反馈进样本库  ② 边校验信号进边库复核队列  ③ 同时返还一篇笔记草稿
```

**第 ③ 条是设计的关键**：用户交出一段话，拿回一篇能直接发出去的笔记。**把"用户帮你的杂活"变成"你送用户的礼物"**——这是回流率能从 <2% 起来的唯一理由。笔记草稿同时成为 M7 笔记层的冷启动供稿来源。

笔记草稿中的车次、价格一律走占位符，复用现有 `validate-ai-copy.mjs` 守门（该校验器一行不用改即可复用）。

---

## 8. 指标体系

### 8.1 北极星

**被采纳的路线数**（用户点击了任一分段的跳转链接 = 一次采纳意向）。

替换掉旧的「方案可用率」。理由：可用率是 QA 门槛不是北极星，且旧定义「分段在数据层可查」是拿自己的答案批改自己的卷子。

### 8.2 护栏指标（必须达标才能上线）

| 指标 | 定义 | 目标 |
|---|---|---|
| **结构准确率** | 抽样方案中「车次真实存在 + 当日有班 + 换乘时间成立」的比例，**由第三方来源核验** | ≥95% |
| 边库时效 | 距上次校验超过 90 天的边占比 | <10% |
| 覆盖诚实度 | 无数据时正确声明的比例 | 100% |

### 8.3 AI 功能指标

| 功能 | 主指标 | 护栏 |
|---|---|---|
| M8 目的地反查 | 反查 → 点「看怎么去」转化率 | 推荐目的地中「已走通」占比 ≥70%；**候选列表外提名率 = 0** |
| M9 对话式回填 | 回填完成率（对比旧表单基线） | 抽取字段用户修正率 <30% |

**M9 的隐藏指标最重要**：每月产生的**有效边校验信号数**。这是社区反哺护城河的唯一度量——用户贡献的不是评论，是供给真伪。

### 8.4 过程指标

边库规模（条）、覆盖 OD 对数、LLM 造边通过率、分享数、笔记草稿采用率。

---

## 9. 验收标准

替换 v0.3 的双验收：

1. **结构复现**：输入「北京 → 喀什」，系统自动产出创始人的原始路线（银川 + 阿克苏两次中转），**且三段车次号全部正确**。
2. **外部核验**：随机抽 20 条产出方案，每段车次/航班拿到 12306 与航司官网核对，结构准确率 ≥95%。
3. **盲测**（v0.2 定了但从未执行）：10–20 名种子背包客，「这条路我之前不知道」的比例 ≥60%，「我会考虑这么走」≥40%。

**注意第 3 条的问法变了**：不问「这个方案可用吗」，问「这是不是你不知道的走法」——因为产品的价值主张变了。

---

## 10. 待验证的前提假设

有一件事必须先验，它独立于以上所有设计：

> **在多数 OD 上，混搭真的比真实可购的最低直飞价有优势吗？**

现有反例已有四条（广州→拉萨、重庆→厦门、西安→丽江、杭州→西双版纳），加上基准偏差修正后，成都→三亚和北京→西双版纳可能也要划掉。

**验证方式**：选 30 条长线，人工查真实最低直飞价 vs 混搭组合，统计成立比例。一周可出结果。

- 若 ≥60% 成立 → 全力做引擎，按本文档执行。
- 若 ≈10% 成立 → 引擎没有用武之地，产品应收敛为「少数几条值得走的长线的深度内容」，AI 用在内容生成与问答上。

**这个实验决定后面所有技术投入是否值得，应该排在一切开发之前。**
