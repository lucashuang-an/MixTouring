# MIXTOURING · 产品共创工作记录

> 本文件记录产品的原始想法、每次讨论后的思路修改与版本演进。所有修改按时间倒序追加，不删除历史。

---

## 0. 产品原点（v0 · 创始人初始想法）

**背景故事（2025 国庆亲历）**：从北京去新疆喀什，直飞乌鲁木齐机票与平台中转方案都太贵，花大量时间人工寻找中转城市，最终路线：北京 🚄（火车）→ 银川 ✈️（飞机）→ 阿克苏 🚄（火车）→ 喀什，**省下约一半费用**。

**初始产品想法**：做一个关于旅行路线与交通方式规划的 App，面向背包客/大学生等个人或小组织旅行者，帮助他们在预算有限的情况下到达各类旅行地。

---

## 1. 版本演进记录

### v0.11.1 · 2026-09-09 · 智谱 key 注入实测：三触点真模型跑通 + 逐任务用量记录上线（当前版本）

**背景**：用户提供智谱 API key，实测 Phase 2 三触点真模型路径，并为后续算法优化建立**逐任务消耗记录**。

**改动**：
1. `server/.env` 注入智谱端点（LLM_BASE_URL=https://open.bigmodel.cn/api/paas/v4，LLM_MODEL=glm-4-flash；不入库）。
2. **用量日志（`server/lib/llm.mjs`）**：每次真模型调用把 `usage`（prompt/completion/total tokens）按 JSONL 追加到 `server/logs/llm-usage.jsonl`（目录已 gitignore）；`callJson` 新增 `kind` 参数（parse/ask/copy），调用方逐一打标，实现按任务类型归因。
3. 触点①③调用方（parse.mjs / qa.mjs）、触点②（generate-ai-copy.mjs --generate）加 kind 标识。

**实测结果（glm-4-flash）**：
- 触点① 解析：「国庆从杭州去喀什」→ 杭州/喀什/2026/10/01，engine=llm，conf 0.95；「明天从上海到昆明」→ 2026/09/10 正确。
- 触点③ 问答：「北京去喀什哪个方案最省钱」→ engine=llm，refs 锚定 p-bjks-2，回答「只需 ¥727」，数字锚定校验通过。
- **触点② 守门实战拦截**：`--generate` 为 2 个 draft 方案调真模型，GLM-4-flash 在 notice 文案手写数字（4、20、60），违反「数字只许占位符」铁律，**三重守门拒绝写盘（数据零污染）**——防幻觉体系对真模型输出实战生效；待优化：重试反馈循环或文案任务换 glm-4-plus（约束遵循更强）。
- `verify.mjs` 真模型环境全绿（PR #2 的断言动态化修复正好兼容）。

**逐任务消耗记录（首次 4 次调用，glm-4-flash）**：
| kind | prompt | completion | total |
|---|---|---|---|
| parse | 267 | 25 | 292 |
| ask | 410 | 36 | 446 |
| ask | 410 | 34 | 444 |
| parse | 266 | 24 | 290 |

**算法优化方向（依据首日数据）**：parse 的 prompt 主要是 24 城 JSON 白名单（~267 tok/次，可规则优先、LLM 只兜底规则未命中）；ask 的 prompt 主要来自 context 注入的方案库事实 JSON（~410 tok/次，可瘦身 planBrief 字段或按路线缓存）；copy 的守门拒绝意味着整次调用 token 浪费（需要带错误反馈的重试或换约束更强的模型）。glm-4-flash 目前免费/低价档，费用以智谱账单为准。

**影响范围**：server/lib/llm.mjs（用量日志）、parse.mjs / qa.mjs / generate-ai-copy.mjs（kind 标识）、.gitignore（logs 目录）；server/.env 与 server/logs/ 均不入库。接口契约与数据层零改动。

---

### v0.11.0 · 2026-09-09 · LLM 真模型接入：智谱 GLM（触点①③切真，Phase 2 三触点全部就绪）

**背景**：Phase 2 的 LLM 三触点（①自然语言搜索解析、②方案 AI 文案、③站内问答）此前一直跑规则版降级路径。用户拿到智谱（GLM）API key，本轮完成注入与真模型切换验证。

**接入方式（零代码侵入）**：
1. 新增 `server/.env`（`LLM_API_KEY` / `LLM_BASE_URL=https://open.bigmodel.cn/api/paas/v4` / `LLM_MODEL=glm-4-flash`），`.gitignore` 已覆盖确认不入库（`git check-ignore` 验证通过）；
2. `package.json` start 脚本改 `node --env-file-if-exists=.env index.mjs`——Node 22+ 原生 env 加载，零依赖，`.env` 不存在（CI/部署无 key 环境）时静默跳过，自动回退规则版，现有降级断言不受影响；
3. LLM 适配层（llm.mjs）本就按 OpenAI 兼容协议实现，智谱 `/chat/completions` + `response_format: json_object` 直接可用，适配层代码零改动。

**真模型暴露的两处 bug（规则版测不出来的那种）**：
1. **问答数字锚定误杀**（qa.mjs）：LLM 正确答「省 ¥1,753」，但锚定校验在 context 原文（含千分位逗号「¥1,753」）里找去逗号后的「1753」找不到，答案被误判幻觉回退规则版。修复：`normNum` 去逗号**双边归一化**后再比对——LLM 输出与库内原文两侧行为一致；
2. **解析日期幻觉**（parse.mjs）：「国庆从杭州去喀什」LLM 凭训练数据猜出 2023/10/01（过去日期）。修复为**双层防护**：prompt 层注入「今天是 YYYY/MM/DD」+「不得早于今天，节日已过顺延到明年」约束 + 示例锚定年份；代码层兜底——LLM 仍返回过去日期时 `while` 循环顺延年份直到不早于今天（防幻觉的最后防线，prompt 被无视也能拦住）。

**verify.mjs 断言环境无关化**：`/api/parse 去玩吧` 用例期望值原硬编码 `llm:false, model:'rule'`，真模型环境天然不符。改为从同环境 p1 响应动态取（`llm: p1.llm, model: p1.model`）——该字段表达「是否注入 key」这一环境事实而非业务契约，断言语义不变。

**实测结果（glm-4-flash）**：触点① parse「国庆从杭州去喀什」→ `2026/10/1`、`llm:true`、日期顺延正确；「明天」→ 次日日期正确。触点③ ask「北京到喀什哪个方案最省钱」→ `engine:llm`，回答含 ¥727 / 省 ¥1,753，数字锚定全部通过。触点②（方案 AI 文案生成）确认属 Phase 3 心愿 agent 工具链范畴：generate-plan.mjs 是「新方案写回器」而非库内文案重生成工具，三重守门与写回入口已就绪，等 Phase 3 agent 产物输入即可跑通。

**验证**：`verify.mjs` 全绿（真模型环境兼容）；`.env` 不入库确认；服务 3000 端口热运行中。

**影响范围**：新增 `server/.env`（不入库）；修改 `server/package.json`（start 脚本）、`server/lib/qa.mjs`（锚定归一化）、`server/lib/parse.mjs`（日期双层防护）、`server/verify.mjs`（断言动态化）。接口契约、数据层、前端零改动；无 key 环境行为与升级前完全一致。

---

### v0.10.2 · 2026-09-09 · 产品方案落地度复查：结果页方案卡补「中转可玩」标识 + 桌面端细节打磨

**背景**：开发期对照《MVP产品文档》v0.3 做了一次功能落地度全面复查。结论：核心闭环（搜索→结果→详情→分享/收藏/回填 + 路线库/我的）已完整落地；确认三类缺口——A 类真缺口（成本低可补）、B 类被「离线 AI 工厂 + 在线规则引擎」架构决策取代的旧设想（在线实时生成/询价失败降级等，文档 §4.2/§5 相应描述视为过期）、C 类按规划后置项（回填校准回流、M6 预算反查、账号体系等）。本轮用户选定补 A 类两项之一（方案卡可玩性标识，§4.3 卡片要素）+ 桌面端继续打磨。

**功能补缺**：结果页方案卡风险四因子行追加「中转可玩」chip（`riskRowHtml(risks, play)`）——`play.city` 有数据时显示陶土橙底 + map-pin 图标 + 「<中转城>可玩」，title 提示详见方案详情；数据源为服务层已透传的 `play` 字段，零新增接口。浏览器实测北京→喀什三卡分别渲染「兰州可玩/银川可玩/西安可玩」。

**桌面端打磨（走查驱动，browser 1280 视口全页走查产出 12 项清单，本轮收敛 5 项）**：
1. 方案/模板详情页分段明细站名 chip 破版修复：固定 `width:56px` 列改为 `min-width:56px` + chip `white-space:nowrap`，长站名（如「乌鲁木齐地窝堡」7 字）不再断字换行，列宽随内容自适应；
2. 「中转可玩」chip 文字色 `#C86A4B`→`#A8543A`：11px 小字在 8% 浅底上对比度 3.4:1 → 约 4.8:1，达次级文字 ≥4.5:1 约定（AGENTS §5.5）；
3. 内容列 720→768px、热门远途网格 `minmax(300px)`→`minmax(220px)`：1280 视口下三卡同排不落单，信息密集页密度提升；
4. 侧栏字标双色渐变断点 58%→32%：原断点落在「MixTo|uring」之间，校正到「Mix|Touring」字符边界（Inter 字宽估算）；
5. 全站 hover 反馈补齐：卡片类（plan/hot/tpl/row/demo-chip）悬停上浮 2px + 阴影加深，按钮类（cta/action/link）上浮 1px + 提亮，排序 pill 未选中项悬停底变白——激活了各页既有 `transition` 声明（原为死代码），仅桌面媒体查询内生效。

**踩坑记录（浏览器自动化）**：沙箱内 `browser_evaluate` 的页面脚本含 `function(){return …}` 会被引擎二次包装吞掉返回值（顶层 IIFE 同理），需改用箭头函数表达式；传输层对含转义双引号/反引号的长脚本不稳，用 `String.fromCharCode(34)` 拼引号规避。

**验证**：`check-pages.mjs` 7 页全绿、`verify.mjs` 全绿；浏览器实测（704 视口，移动布局生效）——三枚可玩 chip 新配色渲染正常、模板详情站名 chip 单行无溢出、全页无横向溢出；HTTP 拉取线上 `desktop.css` 确认全部改动值已生效（32% 断点 / 768px 列 / minmax(220px) / hover 规则组）。桌面视觉态（hover/三列网格/字标渐变）位于 ≥768px 媒体查询内，静态核验通过，待部署前 1280 视口复核。

**影响范围**：`pages/results.html`（riskRowHtml）、`pages/detail.html`（站名 chip）、`assets/desktop.css`（内容列/网格/字标/hover）。DOM 结构、数据层、接口契约零改动。

---

### v0.10.1 · 2026-09-09 · 桌面端改侧边栏布局（Lovable 式：品牌区 + 竖排菜单，当前版本）

**背景**：用户参考 Lovable 桌面布局提出：web 端把底栏导航改为左侧边栏。在 v0.10.0 基础上仅改 `desktop.css`（DOM 仍零改动）：底部胶囊导航 → 左侧 232px 固定侧栏——顶部「MixTouring」双色字标（CSS 渐变文字，Mix 近黑 + Touring 陶土橙），菜单项变图标在左的竖排行（悬停浅灰底、选中态沿用深色底），内容区为侧栏让位（body padding-left）并在剩余空间居中；搜索页页内品牌 Logo 头在桌面端隐藏（`:has(svg[aria-label=MixTouring])` 选择器，me 页「我的」标题头不受影响），移动端断点以下零规则、底栏原样。

**踩坑记录（两个 CSS 兼容点）**：
1. 各页 body 有**内联** `padding:0`，`body { padding-left }` 被内联样式压住 → 侧栏会正常渲染但内容区不让位；内联样式只能 `!important` 覆盖（全文件第二处 !important）。
2. Tailwind v4 的 `-translate-x-1/2` 走独立的 **`translate`** 属性而非 `transform`，`transform: none` 归零不掉 → 侧栏被左移自身宽度 50%（视觉上只剩左半截、菜单项不可见）；需显式 `translate: none`。

**验证**：桌面 1280×800 实测截图——搜索页（字标 + 竖排菜单 + 深色选中行 + 内容居中）、结果页（列表卡 + 直飞基准正常）通过；移动 375×700 回归截图底栏与原版一致。`check-pages.mjs` 7 页全绿、`verify.mjs` 全绿。

**影响范围**：仅 `assets/desktop.css` 重写。DOM、脚本、数据层零改动。

---

### v0.10.0 · 2026-09-09 · 桌面端适配：七页响应式改造（移动优先保持不变）

**背景**：产品页面原为 375px 移动端单一形态，需要电脑端可用。选择**响应式改造现有七页**而非另建 web 版——数据层、api.js、业务逻辑全复用，符合零构建渐进增强原则（AGENTS §7）；本改动属结构级，留版本记录。

**方案**：新增共享样式 `mixtouring-hifi/assets/desktop.css`（断点 ≥768px），七页 `</head>` 前各加一行 `<link>` 引入，**DOM 与业务逻辑零改动**：
- 底部胶囊导航 → 顶部悬浮（保持胶囊形态，宽度随内容收缩）；
- 内容列 430px → 720px 桌面阅读列，为顶栏让位（padding-top 84px）；
- 首页「热门远途」单列卡流 → 自适应网格（auto-fill minmax(300px,1fr)，宽屏 2–3 列），卡片图高 160→140px；
- 日历/城市选择/问问 AI/回填等底册弹层 430→560px。

**实现要点（踩坑记录）**：各页 `.app-shell` 的 430px 列宽定义在 **body 内的 critical-layout 样式块**（晚于 head 中的 link），首版同名选择器被级联顺序压住失效（列宽不生效、Logo 被顶部导航遮挡）；修复为选择器统一加 `body` 前缀提特异性（`body .app-shell` 0,1,1 > `.app-shell` 0,1,0），不依赖级联顺序；`body nav[data-mobile-nav]`（0,1,2）同理稳压 Tailwind 工具类，零 `!important`。

**验证**：浏览器桌面视口 1280×800 实测截图——搜索页（顶栏 + Logo + 720px 卡 + 热门双列网格）、详情页（路线条/AI 解读/分段明细）、结果页（直飞基准 + 方案卡 + 四因子）、路线库（模板卡流）全部正常；移动视口 375×700 回归截图确认底栏导航与原版布局零变化。`check-pages.mjs` 7 页全绿；`verify.mjs` 全绿。

**影响范围**：新增 assets/desktop.css；七页各 +1 行 link。数据层、api.js/app.js、页面 DOM 与脚本零改动。

---

### v0.9.1 · 2026-09-09 · 部署定向：国内优先；CI 守门上线 + Dockerfile 通用镜像

**决策**：产品**先在国内使用**——部署选型从「Render 免费档」调整为**免备案港服优先**（Zeabur 香港容器 或 腾讯云轻量香港，国内访问快、无需 ICP 备案）；Render 降级为海外演示备用。正式规模化运营再迁国内云 + 备案。

**新增**：
- **`Dockerfile`（通用部署镜像）**：node:20-alpine + server 依赖层缓存 + 仅拷贝 server/pipeline/mixtouring-hifi；任何容器平台（Zeabur/Fly/港服 VPS）与自托管均可用。`pipeline/data/` 打进镜像，持久化用卷挂载。
- **`.github/workflows/verify.yml`（CI 守门）**：push/PR 自动起真后端跑 verify.mjs 全接口断言 + check-pages 七页自检（无 key 环境，LLM 路径按降级断言），首跑通过（29s）。Render 类 git-push 自动部署平台的部署前置门。
- **verify.mjs 支持 `VERIFY_BASE`**：同一套断言可直接打远端部署实例（如 `VERIFY_BASE=https://xxx node server/verify.mjs`），部署后线上验证无需改代码。

**影响范围**：新增 .github/workflows/verify.yml、Dockerfile；verify.mjs（+VERIFY_BASE）。render.yaml 保留（海外备用）。代码与数据层零改动。

**部署待办**：选定港服平台 → 部署 → `VERIFY_BASE=<公网地址>` 线上验证 →（可选）配 LLM_API_KEY 切真模型。

---

### v0.9.0 · 2026-09-09 · Phase 3 心愿单异步任务：服务端队列 + 守门写回归一 + 热重载闭环

**背景**：Phase 2 三触点完成后进入 Phase 3。按 AGENTS §6.3 路线：心愿登记 → 服务端任务队列 → 离线生成 → 三重守门写回 → 前端状态回流。遵循 §6.2「离线 AI 工厂 + 在线规则引擎」：**采集执行仍走离线管道**（真实采集需 LLM key 与数据工具，属 Phase 4），服务端承担登记/去重/状态查询与热重载；本版把「登记 → 生成 → 写回 → 回流」的闭环管道全部打通，采集器后续即插即用。

**服务端心愿队列（新增 `server/lib/wishlist.mjs` + 三个接口）**：
- `POST /api/wishlist { from, to, date? }`：登记心愿入队（pipeline/data/wishlist.json，与离线管道共用一存储）；同路线已有 pending/processing 登记时**幂等去重**直接返回既有条目；校验起终点非空/不同、日期格式（YYYY/MM/DD）。
- `GET /api/wishlist`：队列状态列表（pending/processing/generated + generatedAt）。
- `DELETE /api/wishlist/:id`：队列管理（清理误登记；verify 用后自清理不污染数据）。

**守门写回归一（`pipeline/generate-plan.mjs` 重构）**：
- 核心写回逻辑抽为导出函数 `mergeGenerated(store, gen, {verified})`——三重守门（validate-plan 结构 → validate-ai-copy 文案 → 合并后全库校验）任一不过不写盘；CLI 与未来 server 侧写回共用同一实现，杜绝绕过守门的新写入路径（AGENTS §6.2 硬性架构决策）。
- 写回成功后**自动回流心愿状态**：队列中同路线 pending 心愿 → generated + generatedAt，Phase 3 闭环不再依赖人工改 wishlist.json。
- 回归实测：重复方案 id 输入被守门拦截（`p-bjks-1 已存在`）、plans.json 未写盘。

**服务端热重载（`server/index.mjs`）**：plans.json 装载重构为 `loadDB()` + `maybeReload()`——mtime 变化即整库重载（装载 + 双校验 + 派生）；新库校验不过时沿用旧库并跳过该版本（不 fail 整个服务）。挂在 /api/bootstrap 与 /api/wishlist GET 前，离线管道写回后**无需重启**前端即可看到新方案。实测 touch 触发热重载成功。

**前端（状态机不变，仅增强）**：
- `api.js`：`wishRegister`（登记上报，失败静默，同 feedback 模式）、`fetchWishes`（服务端队列状态）。
- `results.html` 空态「让 AI 离线探索」：localStorage 登记的同时 fire-and-forget 同步到服务端队列。
- `me.html` 心愿区块：拉取服务端队列状态合并显示——本地 pending 且服务端 processing 时显示「生成中」（原「探索中」），本地 pending/generated 状态机与自动回流逻辑不变。

**验证**：`server/verify.mjs` 全绿（+7 条心愿队列断言：登记/去重/缺参拒绝/同线拒绝/GET/删除/清理）；`check-pages.mjs` 7 页全绿；generate-plan 守门回归通过；热重载实测通过；wishlist.json 与 plans.json 验证后零残留。

**影响范围**：新增 server/lib/wishlist.mjs；server/index.mjs（+3 心愿接口 + 热重载重构）、server/verify.mjs（+断言）、pipeline/generate-plan.mjs（mergeGenerated 导出 + 心愿自动回流）；api.js（+2 函数）；results.html（+登记上报一行）；me.html（+服务端状态合并 + 引入 api.js）。数据层零改动。

**下一步**：注入 LLM key + 接入数据采集工具后，把队列处理从「离线人工/半自动」升级为 agent 工具链自动执行（collectWish → gen 文件 → mergeGenerated）；或先做 Phase 4 数据源正规化。

---

### v0.8.0 · 2026-09-09 · Phase 2 触点②③：AI 文案生成接 LLM + 站内 grounded 问答

**背景**：v0.7.0 完成触点①后，按 AGENTS §6.3 继续落地 Phase 2 剩余两个触点。继续遵循「LLM key 只存服务端」与「规则版闭环离线可跑」的既定策略（当前无 key，LLM 路径注入即生效）。

**触点② 方案 AI 文案生成（`pipeline/generate-ai-copy.mjs`）**：
- 新增 `--generate` 模式：逐条为缺文案/非 verified 方案调用 LLM（复用 `server/lib/llm.mjs` 的 `callJson`，key 走环境变量），产出**整批过防幻觉守门**（validate-ai-copy 占位符白名单 + 禁手写数字 + 模拟插值无残留）后才写回 plans.json（status=draft，人工 review 改 verified 再 build）——任一条不过全部不写盘。
- prompt 拆为 SCHEMA_PROMPT（角色 + 铁律 + 占位符文档 + 输出 JSON 形状）与方案事实两部分，`--generate` 与原 prompt 文件模式共用同一段指令；顺手修复 `--apply` 对绝对路径参数的拼接 bug（join→resolve）。
- 守门拒绝路径实测：构造含手写数字「23」的产出 → 校验拦截（`p-bjks-1.ai.summary: 含未插值数字 23`）、plans.json 未写盘；无 key 时 `--generate` 优雅退出。当前 11 方案全部有 verified 文案，待新方案入库（如心愿单生成）后即用此路径。

**触点③ 站内问答（grounded QA，`server/lib/qa.mjs` + `GET /api/ask?q=`）**：
- 检索：问题中提到的库内城市 → 命中路线对（两端都提优先，单端次之，最多 2 条）；context 只注入服务层派生事实（直飞基准/方案 id/方式/总价/省额/中转城/verified AI summary），context 本身就是 grounding 源。
- LLM 路径（注入 key 后自动启用）：schema 约束「答案数字必须逐字来自 context，禁止心算/编造」，输出后服务端再做**数字锚定校验**——答案中每个数字串都必须在 context 原文出现、refs 必须是真实方案 id，不过关整条丢弃落回规则版。
- 规则版兜底（当前默认）：命中路线按总价升序列出方案（id/总价/耗时/省额，数字全部来自派生字段）；未命中路线如实说「库里没有」，列出现有覆盖线并引导登记心愿——不编造。
- 前端：`api.js` 新增 `askQ`（问答必须走服务端，离线返回 code:1 不本地编答案）；search 页搜索卡下新增「不确定怎么选？问问 AI」入口 + 问答底册（问题输入 + 回答区 + refs 方案跳转 chips，样式沿用 cal-panel 底册与设计令牌）。

**验证**：`server/verify.mjs` 全绿（+2 条 /api/ask 断言：库内路线 refs 锚定 p-bjks-*、库外问题 refs 为空）；`check-pages.mjs` 7 页全绿；curl 冒烟——「北京去喀什哪个方案最省钱」返回按总价排序的 3 方案与 refs，「附近有什么好吃的」如实兜底。

**影响范围**：新增 server/lib/qa.mjs；server/index.mjs（+/api/ask 路由）、server/verify.mjs（+ask 断言）、pipeline/generate-ai-copy.mjs（+--generate 与路径修复）；api.js（+askQ）；search.html（+问问 AI 入口与底册）。数据层、其余页面、原型冻结区零改动。

**下一步**：注入 `LLM_API_KEY` 后触点①②③全部自动切真模型（契约不变，分别由 schema 约束 + 守门 + 数字锚定三层防幻觉）；随后进入 Phase 3 心愿单异步任务（队列 + agent 工具链 + 三重守门写回 + 前端轮询）。

---

### v0.7.0 · 2026-09-08 · Phase 2 触点①自然语言搜索解析（规则版闭环）

**背景**：Phase 1（薄后端读接口）完成并验证全绿后，按 AGENTS §6.3 进入 Phase 2「LLM 直连三触点」。经用户确认：当前无服务端 LLM key，本次**先落规则版闭环**（key 后续注入即自动切真模型），落地顺序按 AGENTS 既定——先触点①自然语言搜索解析（前端 search 页一句话回填起终点/日期）。

**新增 `server/lib/`（零运行时 LLM，规则闭环离线可跑）**：
1. **`server/lib/llm.mjs`（LLM 适配层）**：唯一 LLM 出口，`callJson({schema_prompt,user})` 走 OpenAI 兼容 `/chat/completions`，`response_format:json_object`；key 仅从环境变量 `LLM_API_KEY / LLM_BASE_URL / LLM_MODEL` 读取（默认 OpenAI/`gpt-4o-mini`），满足「key 只存服务端、浏览器不直连」。`llmConfigured()` 未配置时 `callJson` 直接 resolve null → 调用方走规则兜底；LLM 调用失败同样回退，**在线查询不被 LLM 阻塞**。注入 key 后自动切真模型，响应契约不变。
2. **`server/lib/parse.mjs`（规则版自然语言解析）**：`parseTrip(text,cities)` 优先 `callJson`（LLM 只输出 from/to/date 三字段，城市白名单约束在 system prompt），无 key/失败回退规则引擎——城市词典（24 城）按连接词「从/自/由/在」与「去/到/飞/前往」区分起终点；日期识别今天/明天/后天、本周X、国庆/五一/元旦（农历节假日如春节/中秋不猜、交回 LLM）、显式 `M/D`/`M月D日`。识别不到的字段置 null，**绝不编造**，附 `conf` 置信度与 `engine`（rule/llm）。

**后端路由**：`GET /api/parse?q=` → `{code,data:{from,to,date,conf,engine,note,llm,model}}`；`qstr` 容错缺参返回 code:1。`verify.mjs` 补三条解析断言（国庆从杭州去喀什 / 上海到昆明明天 / 去玩吧无城市不编造）。

**前端 `api.js` + search 页**：`API.parseNaturalLanguage(q)`（同名同契约，后端可达走 `/api/parse`，离线用本地轻量规则 `localParse` 兜底）；search 页新增「或 用一句话说行程」输入行（输入框 + 解析按钮，回车触发），解析成功回填出发地/目的地/日期并 toast，无字段识别时保持原值并提示手动补全（符合「无数据不出卡」精神，不误填）。

**沙箱走查注记**：两次浏览器走查显示前端确实调用 `/api/parse`（Network 中 percent-encoded 中文 query 请求完整）；该沙箱浏览器代理对含中文 query 返回 400，属走查环境网络层异常。`curl localhost:3000` 直连（不受代理影响）编码中文返回 `200 code:0, from=杭州,to=喀什,date=2026/10/01,engine=rule`，后端正确。真实部署下前端 `encodeURIComponent` 编码的 GET 标准 query 无此问题。

**实现注记**：Phase 2 仍保持「LLM key 只存服务端」红线，前端永远拿不到 key；规则版是离线可验证的保底，与 Phase 1 的一次 fetch（api.js）分层一致，页面零结构改动。

**验证**：`node pipeline/check-pages.mjs` 7 页全绿；`node --check api.js` 通过；`node server/verify.mjs` 全绿（含新增 parse 断言）；koa 依赖安装后后端正常启动并装载 24 城/5 路线对/11 方案/6 模板。

**影响范围**：新增 server/lib/llm.mjs、server/lib/parse.mjs；server/index.mjs（+/api/parse 路由 + qstr）；server/verify.mjs（+parse 断言）；server/package-lock.json（npm install koa）；mixtouring-hifi/assets/api.js（+API.parseNaturalLanguage 与本地兜底）；pages/search.html（+自然语言输入行与回填逻辑 + 引入 api.js）；其余页面零改动。下一步：Phase 2 触点②方案 AI 文案生成（运行时校验展示），及用户注入 LLM key 后触点点验转真模型。

---

### v0.6.0 · 2026-09-08 · Phase 1 薄后端读接口完成，原型转真（上版）

**背景**：项目按 AGENTS.md §6.3 路线进入开发期。本次完成 Phase 1：Node 后端（原生 Koa，用户选定）包 `derive.mjs` 为服务层，前端 `api.js` 桩切 fetch，完成标准=七页在真后端下行为与读 mock.js 完全一致。

**新增 `server/`（零多余依赖，仅 koa）**：
- `server/index.mjs`：启动时装载 `pipeline/data/plans.json` → 复用 validate-plan + validate-ai-copy 双校验（fail fast，校验不过拒绝启动）→ `buildServiceDB` 内存派生。接口形状 = mock.js 的 DB 形状：`GET /api/plans?from&to&date`（≙ AGENTS 规划的 /api/routes/:routeId，route_id = `${from}-${to}`）、`GET /api/templates?region`、`GET /api/items/:id`（≙ /api/item/:id）、`POST /api/feedback`（内存接收，反馈权威仍在 localStorage）、另加 `GET /api/bootstrap`（全量 DB 预取）；轻量静态中间件服务七页原型（防目录穿越），统一 {code,data} 错误信封。
- `server/verify.mjs`：完成标准的机器验证——请求全部接口与 `buildServiceDB` 派生结果逐字段深度对比（5 路线对 / 11 方案 / 6 模板 / 地区筛选 / 未命中兜底），全部一致。
- 依赖安装注：`@koa/static` 在当前 registry 不可用，改为手写轻量静态中间件。

**前端 `api.js` 切 fetch（同名同参同返回形状）**：四个 API 函数签名不变，返回 {code,data} 信封与原桩一致；`probe()` 预取 /api/bootstrap 后**原地改写** window.DB（保持页面已捕获引用），file:// 打开或后端不可达时自动回落 mock（离线兜底保留）；feedback 上报失败静默（无账号体系，本地为权威）。

**数据修复（防幻觉校验拦截）**：`plans.json` 中 p-njks-2 的 ai.notice/play_intro 手改残留裸数字「T5」，双校验（build-mock 同链路）拦截。按 §4.2 硬约束改写为无数字表述（航站楼编号属专名，结构化字段 risks.lines 中保留不受限），重新 build mock.js。

**实现注记**：search/loading/me 三页直接同步消费 window.DB（原型分层如此，不引 api.js）；因后端与 mock.js 同一 plans.json + 同一 buildServiceDB 管线产出，数据逐字节一致，行为等价。

**验证**：`node server/verify.mjs` 全绿；`pipeline/check-pages.mjs` 通过；浏览器真后端走查 detail（tpl 详情含 AI 文案与四因子）、results（3 方案卡+直飞基准）、library（6 模板+地区筛选）、share（无购买入口红线 ✓）。

**影响范围**：新增 server/；重写 mixtouring-hifi/assets/api.js（页面零改动）；plans.json 两处 AI 文案修正；mock.js 重新生成。项目托管 GitHub（lucashuang-an/MixTouring，私有，git 代理 127.0.0.1:8890）。

---

### v0.5.0 · 2026-09-08 · 项目转入开发期：AGENTS.md 交接版

**背景**：原型（七页闭环）、数据资产（plans.json 5 路线对/11 方案）、数据管道（判级引擎/防幻觉校验/三重守门）均已完成并验证，用户决定将项目转入 TRAE Code 开发前后端。本次对 AGENTS.md 做开发交接版重写，让开发代理一进来就知道从哪里开始、按什么步骤走。

**决策**：讨论确认「agent 模式可行但分层落地」——LLM 直连（解析/文案/问答）走薄后端 + key 服务端托管，简单且立即可做；数据采集保持离线工厂模式（agent 工具链 + 三重守门），在线查询保持规则引擎。勿做用户在线等待的实时生成（v0.4.4 实测 ~10 分钟/路线）。

**AGENTS.md 交接版新增内容**：
- **§6 开发交接**：进入项目必读顺序（AGENTS.md → 方案库Schema.md → mock.js（API 响应契约）→ derive.mjs（业务规则唯一实现）→ api.js（签名基准））；已验证架构决策清单（勿推翻）；四阶段路线；资产转正映射表（derive.mjs 直接移植勿重写、plans.json 换 DB 保持字段形状、mock.js 即接口契约等）。
- **§2 目录结构**：补齐 pipeline/ 全部文件与角色标注。
- **§4.2 新增硬约束**：AI 文案防幻觉——数字必须来自占位符插值，LLM 输出含手写数字即拒绝入库。
- **§7 工程约定**：明确技术栈（前端零构建渐进增强、后端原生 Koa/Express）、开发代码新建 server/ web/ 目录不混入原型目录。
- **§8 经验教训**：扩充 location.replace 返回栈、数据源冲突兜底、淡季混搭未必省钱三条实测教训。

**四阶段开发路线**：Phase 1 薄后端读接口（derive.mjs 包成 4 个 GET，api.js 切 fetch，完成标准=七页行为与读 mock.js 完全一致）→ Phase 2 LLM 直连三触点（自然语言搜索/文案生成过校验/站内问答）→ Phase 3 心愿单异步任务（队列 + agent 工具链 + 守门写回）→ Phase 4 数据源正规化（聚合商 API 或策展库）。

**影响范围**：仅 AGENTS.md 重写 + 本记录；代码、原型、数据零改动。原型期资产全部保留并冻结为开发基准。

---

### v0.4.6 · 2026-09-08 · 心愿单回流实例：南京→喀什离线生成完成

**背景**：用户在「我的」心愿单里看到「南京 → 喀什 · 探索中」，询问是否已生成。该路线此前不在方案库，本次按 v0.4.5 的异步流程完成离线生成并回流。

**生成结果**（全部真实采样，来源与时间戳随数据入库）：
- **直飞基准**：南京 → 喀什 MU2963，6h15m，¥1,400（Trip.com HK 最低 HK$1,239 ≈ ¥1,140，携程全价 ¥3,690，取 10 月初非全价典型值）。
- **p-njks-1（火车+飞机 · 夜卧铺西安中转）**：Z164 南京 21:19 → 西安 09:19+1（硬卧 ¥262）→ 机场巴士 70min → 春秋 9C6381 西安 13:55 → 喀什 18:40（折扣舱样本带 ¥600–1,000）。总 21h21m，¥862–1,262，比直飞省 ¥338。判级：衔接中 / 行李中 / 退改中 / 接驳中（西安站 40km 跨站机场）。
- **p-njks-2（飞机+飞机 · 西安同航站楼中转）**：MF3449 南京 07:35 → 西安 09:35（¥485）→ 9C6381 西安 13:55 → 喀什 18:40（¥600–1,000）。同 T5 航站楼，衔接 4h20m。总 11h05m，¥1,085–1,485，比直飞省 ¥115。判级：衔接低 / 行李中（两段大概率无直挂）/ 退改中 / 接驳低。

**执行路径**：
1. 用户端心愿单保持「探索中」（[me.html](computer://d:\\VIBE CODING\\MIXTOURING\\mixtouring-hifi\\pages\\me.html) 读取 DB.routes 自动回流）。
2. 离线侧通过真实网络检索完成数据采集（南京↔喀什直飞、南京→西安高铁/卧铺/飞机、西安→喀什航班/低价），写入 [pipeline/out/gen-nanjing-kashgar.json](computer://d:\\VIBE CODING\\MIXTOURING\\pipeline\\out\\gen-nanjing-kashgar.json)。
3. 经 `pipeline/generate-plan.mjs --verified` 三重守门写回 [plans.json](computer://d:\\VIBE CODING\\MIXTOURING\\pipeline\\data\\plans.json)，`pipeline/build-mock.mjs` 重建 [mock.js](computer://d:\\VIBE CODING\\MIXTOURING\\mixtouring-hifi\\assets\\mock.js)。
4. [wishlist.json](computer://d:\\VIBE CODING\\MIXTOURING\\pipeline\\data\\wishlist.json) 中南京-喀什状态由 pending 改为 generated。

**验证**：7 页静态自检全绿；mock.js 中南京-喀什两方案 AI 文案插值正确；心愿单状态已回流。

**影响范围**：plans.json +1 路线对 +2 方案（5 路线对 / 11 方案 / 14 份 AI 文案）；mock.js 重建；wishlist.json 状态更新；页面零改动。下一步：继续按 wishlist 中的广州→拉萨生成，或进入 P2 自然语言搜索。

---

### v0.4.5 · 2026-09-07 · 心愿单机制：随机输入无数据路线的异步生成闭环

**背景**：v0.4.4 实验证实实时生成约 10 分钟/路线、不适合在线等待，结论指向「心愿单登记 → 离线生成 → 回来看结果」。本次把该闭环落地到原型与管道两侧。

**用户侧（原型）**：
- **登记**（results.html 空态）：无数据路线不再只有「去看别的」，新增「让 AI 离线探索这条线」主按钮——登记后变「已在探索中」，附提示「生成后可在我的查看进度」；若该路线已生成过（如心愿回流后再次搜索），按钮变「已生成，点此查看」。空态示范路线第二条换为 成都→乌鲁木齐（呼应 v0.4.4 新路线）。
- **进度与回流**（me.html 新增「我的心愿」区块）：pending 显琥珀「探索中」、generated 显绿「已生成」+ 时间，点击直接进 loading→results 看方案；**自动回流**——打开页面时 pending 心愿若已在方案库（DB.routes）中出现即自动升级为已生成，无需任何后端同步，闭环在纯静态原型内成立。
- **存储**（app.js）：`mt:wishlist` localStorage 键 + wishList/wishAdd/wishDismiss/wishMarkGenerated/wishIndex 五个工具（同路线去重、dismiss 软删除）。

**管道侧（Node）**：
- `pipeline/wishlist-store.mjs`：心愿 JSON 读写（与 generate-plan 解耦）。
- `pipeline/process-wishlist.mjs`：离线处理框架——默认模式把 pending 心愿输出为 `out/gen-prompts.json` 生成任务（含结构/来源/占位符约束的 prompt + 路线是否已存在的标记），交 LLM+采集生成后走 v0.4.4 的 generate-plan 守门写回；`--generate` 为模拟状态流转（不真跑爬虫，如实标注 simulated）。
- `pipeline/data/wishlist.json`：心愿库（当前含一条示范 pending：广州→拉萨），`out/gen-prompts.json` 为其生成任务样本。

**原型/管道衔接说明**：MVP 无后端，浏览器 localStorage 心愿与管道心愿库的同步是人工的（运营侧收集登记后录入 wishlist.json，或后续加导出）；「已生成」的判定不依赖同步——前端以方案库是否出现该路线为准。

**验证**：wishAdd 去重 / 状态流转冒烟测试通过；7 页静态自检全绿；build-mock 回归通过（12 份 AI 文案、4 路线对、9 方案）。

**影响范围**：app.js（+心愿工具）、results.html（空态 CTA）、me.html（+心愿区块）、pipeline/（+wishlist-store/process-wishlist/data/wishlist.json）；mock.js 与既有页面数据零改动。下一步：扩库（按 gen-prompts 批量生成高频背包客路线）或 P2 自然语言搜索。

---

### v0.4.4 · 2026-09-06 · 实时生成实验：成都→乌鲁木齐 端到端跑通

**背景**：用户提出「随机输入起终点、方案库没有现成方案时能否实时生成」。v0.4.1 架构判断实时生成（LLM+浏览器）延迟与失败率不可接受，本次以实验验证真实成本与可行性，同时把实验过程固化为可复用的写回管道。

**实验对象**：成都 → 乌鲁木齐（方案库没有的随机路线，实测端到端 **约 9.5 分钟**：00:06 开始采集 → 00:16 构建上页；搜索级采集 7 次检索 + 1 次页面抓取）。

**生成结果**（全部真实采样，来源与时间戳随数据入库）：
- **p-cdwl-1（混搭）**：K2616 成都西 20:42→兰州 08:10（硬卧 ¥208，ailieche）→ 城际 50min 接驳 → CZ6675 中川机场 11:20→乌鲁木齐 14:10（折扣舱样本带 ¥650–1,000，5 个样本）。总 17h28m，¥858–1,208，比直飞（¥1,210 基准）省 ¥177。判级：衔接中 / 行李中 / 退改中 / 接驳高（70km）。
- **p-cdwl-2（直达火车）**：K2058 成都西 19:58→乌鲁木齐 D3 09:19，37h21m，硬卧 ¥492，省 ¥718。判级：行李低 / 退改中（票源紧张）+ 长途硬卧提示（advisory）。
- 两方案 AI 文案占位符校验通过，status=verified 上页；搜索「成都→乌鲁木齐」即可查看。

**新增工具**：
1. **`pipeline/lib/validate-plan.mjs`**：方案结构一致性校验器——stops/segs 时刻互锁、衔接 wait_min 与班次差核对、时长与时刻差核对、价格二选一、风险因子齐全（**直达方案无中转站时放宽 connection/transfer**，本次实验驱动的设计改进）。已接入 build-mock 构建前置，旧库 10 份方案回归全过。
2. **`pipeline/generate-plan.mjs`**：生成写回器——三重守门（结构 + AI 文案 + 合并后全库校验）任一不过不写盘；默认 AI 文案强制 draft，`--verified` 标记会话内已人工 review。

**实验发现（实时生成的真实成本）**：
1. **延迟**：搜索级采集约 10 分钟/路线，浏览器级 OTA 采集会更慢更脆 → 证实实时生成不适合在线等待，适合「心愿单 + 异步生成 + 回来看结果」。
2. **数据源冲突是常态**：K2058 到达时刻三个来源三个版本（08:02 / 09:19 / 09:33），票价同理；需要「最新鲜来源优先 + sampled_at 时间戳 + 来源字段随库」的策略（已入 gen 文件 source 字段）。
3. **淡季混搭不一定省钱**：9 月成都直飞乌鲁木齐 ¥989 起，混搭仅省 ¥177；省钱叙事之外，AI 文案要转向「卧铺体验 + 中转城市玩法」价值。方案库如实展示，不粉饰。
4. **判级规则 v2 候选**：接驳因子对「距离远但有城际直达航站楼」的机场偏严（兰州 70km 判高，实际 50 分钟轨交直达），候选规则 `rail_direct && min ≤ 60 → 中`，待 v0.5 评估。

**影响范围**：plans.json +1 路线对 +2 方案（9 方案 / 12 份 AI 文案）；mock.js 重建；页面零改动（新路线自动出现在城市选择与生成仪式文案中）；`pipeline/out/gen-chengdu-urumqi.json` 留存为生成样本。下一步：心愿单机制（随机输入 → 登记 → 异步生成 → 「我的」页看结果）。

---

### v0.4.3 · 2026-09-06 · P1 落地：生成仪式接方案库事实

**背景**：v0.4.2 完成 P0（AI 解读文案上页）后，按既定顺序落地 P1「生成仪式」——loading 过渡页的步骤文案原本是写死的通用话术，与真实数据无关，用户感知不到「AI 在用真实数据干活」。

**改动**（仅 `mixtouring-hifi/pages/loading.html`，其余零改动）：
1. **步骤文案数据驱动**：路线在方案库有数据时，四步全部带真实事实——「检索 {from} 出发的班次与票价样本」→「在 {真实中转城市} 匹配中转组合」（中转城市从该路线各方案 stops 提取去重，>3 城显示前 2 + 等N城）→「评估 {方案数} 个候选的四因子衔接风险」→「对比直飞基准 {直飞价}，按总花费排序」。
2. **完成态节拍**：进度条 100% 时标题从「AI 正在规划路线」切换为结果句——有数据「已找到 N 个省钱方案」，无数据「这条线暂无组合票数据」；停 650ms 后 `location.replace` 进结果页（仍不进返回栈）。总时长 2450ms → 2950ms。
3. **无数据不编造**：方案库没有的路线保持原通用四步（描述过程而非结果），完成态如实说无数据，硬约束「无数据不出卡」在仪式层同样成立。

**验证**：三种情形冒烟测试通过（北京-喀什：银川/阿克苏等4城、3候选、¥2,480 直飞基准；上海-昆明：贵阳/杭州、2候选、¥1,260；北京-广州：通用步骤 + 无数据提示）；`check-pages.mjs` 7 页全绿；文案中数字全部来自 DB 字段（城市数/方案数/直飞价），无手写数字。

**影响范围**：仅 loading.html 单文件增量，动画节奏与 UI 令牌零改动；下一步 P2 自然语言搜索（search 页接自然语言输入解析 → 回填起终点/日期）。

---

### v0.4.2 · 2026-09-06 · P0 落地：方案库数据管道 + AI 解读触点上页

**背景**：v0.4.1 定方向后，本次把 P0（方案库 schema → 构建管道 → 前端 AI 触点）完整落地：数据从手写 mock 迁移为「存储层 plans.json → 构建派生 → 服务层 mock.js」的单向管道，AI 解读卡挂上详情页与列表页。

**新增**：
1. **`pipeline/data/plans.json`**：8 方案（7 计划方案 + 6 模板）规范化存储层数据——stops/segs/risks 四因子原始值/price_band 采样带/AI 种子文案（占位符写法，status=verified）；顺带修复了旧 mock 三处内部不一致（p-bjks-3 / p-bjwl-2 / p-shkm-2 的时刻与衔接时长矛盾，以规范化数据为准）。
2. **`pipeline/lib/derive.mjs`**：存储层→服务层派生引擎——四因子判级规则 v1 代码化（跨站衔接扣除接驳 30 分钟缓冲再判级）、时间/价格格式化、AI 文案占位符插值。
3. **`pipeline/lib/validate-ai-copy.mjs`**：防幻觉校验器——占位符白名单 + 去占位符后禁现阿拉伯数字，build 前置拦截。
4. **`pipeline/build-mock.mjs`**：一键构建 `mixtouring-hifi/assets/mock.js`（生成文件禁手改，头部标注数据版本与生成时间）。
5. **`pipeline/generate-ai-copy.mjs`**：AI 文案工厂脚手架——为缺文案方案生成 LLM prompt（`pipeline/out/`）；`--apply` 写回时三重校验（白名单/禁手写数字/占位符试插值无残留），draft 状态不进服务层，人工 review 改 verified 后构建才上页。
6. **`pipeline/check-pages.mjs`**：7 页静态自检脚本（内联脚本语法 / DOM id 引用 / sprite 图标引用 / 跳转目标 / 购买入口红线词），此前散在会话里的自检动作固化为可复跑工具。

**页面改动**（遵循已定稿 UI 令牌，无 verified 文案不出卡）：
- `detail.html`：新增 **AI 解读卡**（sparkles 标识 + summary 卖点 + fit 适合谁 + notice 琥珀提醒条，数字全部来自数据插值）；「中转顺便玩」卡头注入 AI play_intro 引子；sprite 补 `hero-exclamation-triangle`。
- `results.html`：方案卡在路线条下方挂 **AI summary**（两行 clamp，无文案方案不渲染该行）。

**判级口径变化**（规则引擎派生取代手写，向更严方向）：p-bjks-1 银川衔接 中→高（113min 窗口扣 40min 接驳 + 30min 缓冲仅余 43min）、接驳 低→中（18km 跨站）；p-bjwl-2 银川衔接 低→中（有效余量 90min 赶飞机）。判级理由逐条展示原始值与规则行，无总分，符合硬约束。

**验证**：构建通过（24 城市 / 3 路线对 / 7 方案 / 6 模板，AI 文案 10 份校验通过、无占位符残留）；apply 错误路径实测拦截手写数字、幻影占位符 `{transfer_city_3}`、不存在方案三类问题；`check-pages.mjs` 7 页全绿；服务层形状与前端消费字段逐项核对兼容（`priceMid` 排序、`hours()` 正则、`favSnapshot` 等零改动）。

**影响范围**：`mixtouring-hifi/assets/mock.js` 改为生成文件（头部有警示注释）；`detail.html` / `results.html` 增量；其余 5 页与 `prototype/` 零改动；产品定位（纯内容、无购买入口、无数据不出卡）保持不变。下一步：P1 生成仪式（loading 页步骤文案接方案库事实）。

---

### v0.4.1 · 2026-09-06 · 进入阶段 1：数据策略与 AI 架构定向，产出方案库 Schema v0.1

**背景**：v0.4.0 原型定稿后进入开发流程。经架构研究确认：LLM+浏览器采集不适合在线查询路径（延迟 10–60s/次、约 $1.45/千页 token 成本 + 代理与浏览器基础设施、OTA 反爬导致随机失败），且火车票价为固定定价（仅需时刻表+固定票价表，低频维护）、全管道仅机票价格需采样；机票侧无免费官方实时报价 API（聚合服务按量付费且有下架先例，飞猪开放平台需商家资质）。

**决策**：
1. **数据策略 = 离线采样样本库**：LLM+浏览器采集放**离线管道**（每日批量），方案库预计算；在线服务纯规则（总价带排序 + 四因子判级），毫秒级、零 LLM 成本、可解释。
2. **AI 双层架构**：离线「AI 工厂」（LLM 采集 / 路线发现 / 文案生成）+ 在线四个可感知触点（自然语言搜索 → 生成仪式 → AI 方案解读 → 问问 AI），方案库是 AI 劳动的资产形态。**防幻觉红线：数字只来自数据库字段，LLM 只写叙述**，文案中数字一律占位符插值 + 入库前校验。
3. **前端静态原型直接演进**（api.js 桩形状即后端契约，UI 零改动换真数据）；MVP 继续匿名 + localStorage。
4. **AI 触点落地顺序**：P0 AI 解读文案（离线生成+详情页展示）→ P1 生成仪式 → P2 自然语言搜索 → P3 问问 AI。

**新增**：`方案库Schema.md`（v0.1）——存储层/服务层分层 schema：RoutePair/Plan/Segment/PriceSample/RiskFactor/AICopy/Template 七类实体、核心四因子判级规则表 v1、存储→服务映射规则（与前端现消费形状 1:1）、四接口映射、P0 落地清单。

**影响范围**：纯文档增量，`mixtouring-hifi/` 与 `prototype/` 零改动；mock.js 数据后续按 schema 规范化（risks 补核心四因子 + advisory 单列、price 拆 min/max）；合规边界——采样保持低频（每路线每日数次）、保留「估算价格样本」页脚口径、不做交易。

---

### v0.4.0 · 2026-09-06 · mixtouring-hifi 补全为完整用户链路可交互原型

**背景**：v0.3.7 定稿的 `mixtouring-hifi/` 只有 search / library / detail 三个静态页面，用户主链路（搜索 → 生成 → 方案列表 → 详情 → 分享/收藏/回填）断裂，且数据全部硬编码在页面里。本次在不改动已定稿 UI 的前提下，把原型补全为覆盖完整用户旅程的可交互版本。

**新增/改动**：
1. **新增 4 个页面**：`pages/loading.html`（AI 生成中过渡页，步骤动画 + 进度条，结束后 `location.replace` 跳结果页，不进返回栈——沿用 v0.3.5 教训）、`pages/results.html`（方案列表：直飞基准卡 + 方案卡 + 预算/耗时/均衡排序 + 无数据空态引导）、`pages/share.html`（分享落地页：方案摘要 + 复制链接，**无购买入口**，页脚明示不提供购票服务）、`pages/me.html`（我的：收藏列表 + 回填实测记录，localStorage 驱动）；另加 `index.html` 入口重定向到搜索页。
2. **新增共享层** `assets/`：`mock.js`（集中 mock 数据：城市列表、3 条有方案路线、6 条路线库模板）、`api.js`（API 桩层，`planSearch / fetchTemplates / fetchItem / postFeedback` 四个接口形状即未来真实 API，接后端只换实现）、`app.js`（MT 工具层：收藏/回填存储、URL 参数、底部导航统一接线、Toast、转义、风险判级配色）。
3. **改造既有三页**：search 页接入城市选择底册、日期选择、URL 参数预填（模板一键复用入口）、热门远途卡数据驱动 + 收藏；library 页地区筛选 chips 走 API 桩；detail 页全面数据驱动（路线条/分段/风险四因子/操作区），模板与方案双模式（`?tpl=` / `?id=`），收藏/分享/回填实测底册/一键复用全部接线。
4. **自检修复**：详情页底部导航高亮原来写死在「搜索」，现按来源动态切换（`?tpl=` 高亮路线库、`?id=` 高亮搜索）；共享 sprite 补齐 `hero-heart-solid / hero-link / hero-map-pin / hero-x-mark` 四枚缺口的 symbol。

**静态自检结论**（对照验收清单全过）：7 页内联脚本语法校验全通过；所有 `getElementById` 目标存在；逐页图标引用均有内联 symbol 定义；页面间跳转目标全部有效；空态/无效数据兜底（结果页空态给 3 条有数据的示范路线、详情页「没有找到这个方案」、分享页「分享已失效」）；硬约束复核——无购买入口 ✓、无数据不出卡 ✓、风险四因子原始值各自展示无总分 ✓、模板复用只带起终点、日期用户重选 ✓、回填仅存本机 ✓。

**影响范围**：`mixtouring-hifi/` 从 3 页静态稿升级为 7 页 + 共享层的可交互原型（仍纯静态、无构建步骤、单页可独立打开）；数据层集中后页面内零硬编码数据；`prototype/` 存档不动；产品方案与已定稿 UI 零改动。

---

### v0.3.8 · 2026-09-06 · AGENTS.md 去 UI 细节硬约束，释放微调空间

**背景**：创始人认为 AGENTS.md 把配色/字体/图标/组件质感等 UI 细节列为「硬性约束（违反即返工）」会阻碍后续对产品的灵活微调——UI 本就是可迭代的，产品本质的规则才应是红线。

**调整内容**：
1. **§4 硬性约束** 只保留产品本质层面的三条：**定位与交易边界**（纯内容、不做买票、无购买入口）、**数据与方案卡**（无数据不出卡、风险四因子、模板复用结构）、**版本与文档**（Markdown、倒序留痕）。
2. UI 视觉细节整体移至新增的 **§5「当前设计参考」**（主题配色/字体/图标/组件布局），明确标注「可微调、非硬约束」——小改可直接动手，重大调整（换主题/换字体库）才需在 `工作记录.md` 留版本记录。
3. **§6 工程约定** 同步放宽：`mixtouring-hifi/` 为当前主版本，UI 微调可直接进行，不再要求"任何改动必先立版本"。
4. 章节顺延：原 §5 工程约定 → §6，原 §6 经验教训 → §7。

**影响范围**：仅 `AGENTS.md` 结构与措辞调整；`mixtouring-hifi/`、`prototype/`、产品方案全部零改动。产品本质硬约束（不做交易、无购买入口、不编造数据、版本留痕）保持不变。

---

### v0.3.7 · 2026-09-06 · 最终 MVP 原型定稿为 mixtouring-hifi + 主题规范更替

**背景**：创始人确认 `mixtouring-hifi/` 文件夹内容为**最终版 MVP 原型**（直接沿用，不做修改）。该原型由 HIFI 生成管线产出，含三个移动端页面（375px 视口）：`pages/search.html`（搜索首页）、`pages/library.html`（路线库）、`pages/detail.html`（方案详情），页面间以导航交互串联；资源含 Heroicons sprite 与三张路线实拍图。

**创始人决策（采纳为定稿）**：
1. **最终 MVP 原型 = `mixtouring-hifi/`**：原样沿用、不再改动；`prototype/` 下两份原型（mvp-mobile.html / mvp-prototype.html）转为历史版本保留存档。
2. **主题规范更替**：HIFI 采用的「Warm Modern Minimalist」暖调极简配色（陶土橙 `#C86A4B` 点缀、近黑 `#111111` 主色、`#F8F9FA` 画布 + 白卡、琥珀 `#FBBF24` / 红 `#E11D48` / 绿 `#34C759` 状态色）**取代** v0.3.4 定稿的「墨蓝远行」（`#1e3a5f` / `#3e92c4` / `#e4ebf4`）；旧主题仅保留于历史原型。
3. **设计令牌唯一准绳**：`mixtouring-hifi/colors_and_type.css`（配色 / 圆角 8-24px / 柔和投影 / 8pt 间距网格 / Inter + Noto Sans SC 400·500）。

**一致性核对**：
- 与既有定稿一致——字体方案 A（Inter + Noto Sans SC 400/500，全站无加粗 ✓）、图标库 Heroicons（sprite 形式 ✓）、无购买入口（已全量检索确认 ✓）。
- 发生变更——配色主题（见上）；组件质感（HIFI 引入柔和投影与大圆角卡片，旧「发丝线 / 无边框无阴影」轻量化细则不再约束新原型，仅适用于历史原型维护）。

**影响范围**：AGENTS.md 已同步更新（§2 目录结构、§4.1 主题色、§4.4 轻量化适用范围、§5 工程约定）；`mixtouring-hifi/` 文件零改动；`prototype/` 保留不动。数据与方案卡规则（§4.5 无数据不出卡、风险四因子、分享页无购买入口）等产品约束全部保持。

---

### v0.3.6 · 2026-09-05 · 设计系统定稿：字体方案A + 图标库 Heroicons

**背景**：创始人认为原型字体与图标组件需要优化，经调研参考（见 `prototype/font-icon-reference.html`）后作出选择。

**创始人决策（采纳为定稿）**：
1. **字体 = 方案 A**：Inter（400/500）+ Noto Sans SC（400/500），Google Fonts CDN 引入，降级栈方向为系统字体。中英混排跨设备渲染一致；字重仅用 400/500，符合全站 no-bold 约束。
2. **图标库 = Heroicons**：292 枚手绘图标，线性（1.5px 描边）+ 实心双变体。删除当前手写内联 SVG（搜索/地图标记/交换/AI 星芒/用户等），改用 Heroicons 同名图标以 `currentColor` 继承，选中态切 `--accent`。1.5px 细描边与全站 0.5px 发丝线边框风格协调。

**方案权衡**（调研纪要）：首推候选原为 Lucide（1,500+ 图标覆盖广），但创始人选定 Heroicons 的精细细描边，更贴 Apple 轻量化规范；图标数量 292 枚对本产品功能面足够，无需更全的 Tabler/Phosphor。

**影响范围**：设计系统（字体栈 + 图标规范）；待落地到 `prototype/mvp-mobile.html` 与 `prototype/mvp-prototype.html` 的样式层与图标替换；数据层、交互逻辑、产品方案不变。

---

### v0.3.5 · 2026-09-01 · 修复：搜索后按返回卡在「生成中」页

**Bug 复现**：首页点「出发!」搜索 → 方案列表页 → 按左上角「‹ 搜索」返回，页面停在「AI 正在规划路线…」的生成中界面，而不是回到搜索页。

**根因**：导航栈使用 `show(id, push)` 管理，凡是非根页面都会默认压入栈。`doSearch()` 里 `show("v-loading")` 把临时「生成中」页也压入了栈，随后 `renderList/renderEmpty` 再压入方案列表页，栈变为 `[v-search, v-loading, v-list]`。返回时按栈弹出，先弹到 `v-loading`，于是卡在生成中界面。

**修复**：`show("v-loading", false)` 显式不压栈（`push===false` 时跳过 `stack.push`）。生成中页是纯瞬态过渡页、无返回入口，本就不应出现在返回链路里。修复后栈为 `[v-search, v-list]`，返回直接回到搜索页；详情 → 列表 → 搜索的逐级返回链路不受影响。

**影响范围**：仅 `prototype/mvp-mobile.html` 的 `doSearch()` 一处调用；数据层、样式不变。

---

### v0.3.4 · 2026-09-01 · 主题色方案④「墨蓝远行」落地 + 全站轻量化

**背景**：首页改版（搜索整合单卡片、热门远途左图右文、去介绍文案）后，创始人从 5 组双色递进色卡中选定方案④，并提出三条全局规范：UI 内字体一律不加粗（含按钮）、按钮与组件朝轻量化设计、成对排列的文字场景居中。

**主题色（2 色相 · 饱和度递进）**：
- 主色（深墨蓝）`#1e3a5f`：主按钮、强调文字、火车线、tab 选中态
- 点缀色（亮青蓝）`#3e92c4`：AI 语义、链接、飞机线、@贡献者
- 浅阶 `#e4ebf4`：选中底色、焦点环、图标底
- 基底：背景纯白 `#ffffff`，卡片浅灰 `#f5f5f7`（卡片靠底色成形，无边框无阴影）

**本轮改动（已落地到 `prototype/mvp-mobile.html`）**：
1. **配色翻转**：`--bg`/`--card` 互换为白底灰卡；navbar/tabbar 磨砂白；输入框、交换钮、徽标改为白色浮于灰卡之上；热门路线 4 张缩略图统一为蓝色系饱和度递进渐变（去除绿/橙杂色）；分段图标底色去绿调统一 `--accent-soft`。
2. **全局去加粗**：新增 `b,strong{font-weight:400}` 兜底规则，连同已有逐类声明，全站（含按钮、价格、标题、徽标）无任何粗体。
3. **轻量化**：灰卡去边框去阴影；主按钮 padding 13→12px、出发按钮 13→11px；输入框 padding 12→11px；分段控件选中态阴影减弱（.12→.08）；描边按钮 1px→.5px 发丝线；小按钮内边距收紧。
4. **成对文字居中**：出发地⇄目的地标签与输入值居中、日期居中（已在首页改版中落地，本轮复核保持）。
5. **顺手修复**：路线条城市名样式选择器错位（CSS 写 `b` 而 JS 输出 `span`，城市名一直渲染为默认 15px，改为匹配 `span` 的 12.5px）；路线库筛选 chips 补上 `.on` 选中态样式（此前选中无视觉反馈）；方案列表「直飞基准」卡去掉硬编码底色 `#f9f9fb`，统一灰卡。

**影响范围**：仅 `prototype/mvp-mobile.html` 样式层与两处文案外的静态结构；数据层、交互逻辑不变。设计规范（Apple 风格、系统字体栈、风险四因子、分享页无购买入口）全部保持。

---

### v0.3.3 · 2026-09-01 · 手机版 UI/UX 系统化审计与修复：Harden 收口轮

**背景**：v0.3.2 视觉打磨后，引入 OpenDesign（nexu-io/open-design）开源技能库的审查方法学，对 `prototype/mvp-mobile.html` 做一轮系统化审计——`impeccable-design-polish` 的 Audit/Harden 模式（层级、间距、交互态、响应式、可访问性）、`apple-hig` 的触控目标规范、`review-animations` 的动效十诫。

**审计发现（问题清单）**：
1. 🔴 **数据丢失 bug**：`store()` 函数的读取调用误传了默认值 `[]`，导致每次读取收藏/回填记录都会先清空 localStorage——收藏永远只保留最后一条，「我的」页数据无法累积。
2. 🟠 **假控件**：方案列表的排序分段控件（预算/耗时/均衡）点击无任何效果；搜索页「偏好」分段控件也未联动到任何行为。
3. 🟠 **触控目标不达 HIG 44pt**：返回按钮约 28px、起终点交换按钮 34px、筛选 chips 约 30px。
4. 🟡 **对比度不足**：次级文字色 `#8a8a8e` 在白底上仅 3.5:1，低于 WCAG AA 的 4.5:1。
5. 🟡 **交互态缺失**：`select` 下拉无焦点环（input 有）；交换按钮、分段控件无按压反馈；全站无键盘焦点样式。
6. 🟡 **动效违规**（review-animations 十诫）：toast 使用 `transition:all`（无边界属性动画）；无 `prefers-reduced-motion` 降级。
7. 🟡 **类型尺度不一致**：首页大标题 27px（行内覆盖）vs 路线库/我的页 32px；路线条微标签 10–10.5px 过小。

**本轮改动（已落地到 `prototype/mvp-mobile.html`）**：
1. **修复数据丢失**：`store()` 读取调用改为无参读取（isFav / toggleFav / renderMe / 回填提交共 5 处），收藏与回填记录现在正确累积、跨刷新保留。
2. **假控件变真**：实现列表排序（价格取区间下限解析、耗时按小时、均衡为价格+耗时+风险罚分的综合分）；搜索页「偏好」在生成时带入列表初始排序，列表上仍可二次切换。
3. **触控目标达标**：交换按钮 34→44px 并与输入框底对齐；返回按钮 min-height 44px；chips 提高内边距至 38px；分段控件 min-height 40px。
4. **对比度**：`--ink2` 调整为 `#6e6e73`（白底 5.1:1、灰底 4.7:1，均过 AA），保留 Apple 灰调。
5. **交互态补全**：`select` 获得与 input 一致的焦点环；交换按钮/chips 增加按压底色反馈；新增 `button:focus-visible` 键盘焦点轮廓。
6. **动效合规**：toast 改为仅过渡 `opacity` + `transform`；新增 `prefers-reduced-motion` 降级（骨架屏微光关闭、加载圈减速、按压缩放与 toast 过渡取消）。
7. **类型尺度统一**：大标题统一 28px（移除首页行内覆盖）；路线条微标签 10/10.5→11px。
8. **安全区**：navbar 增加 `safe-area-inset-top` 处理，兼容全面屏 standalone 场景。

**影响范围**：仅 `prototype/mvp-mobile.html`；交互链路新增排序行为，数据层、产品方案不变。设计规范（Apple 风格、系统字体栈、风险四因子、无购买入口）全部保持。

**下一步**：创始人过一遍手机版（重点：排序联动是否符合直觉、收藏/回填是否按预期累积）；满意后可进入「对话化搜索」或方案卡信息层级精简的下一轮迭代。

---

### v0.3.2 · 2026-08-31 · 手机版 UI 自检与第一轮打磨：补 Logo + AI 原生简洁性

**背景**：创始人要求对手机版做设计自检，重点是①品牌 Logo 缺失；②作为 AI 原生产品的简洁性是否体现。目标是把手机版 UI 做到「基本满意」，进入迭代打磨阶段。

**自检结论（问题清单）**：
1. Logo 缺失：主屏顶部只有裸标题，品牌标识仅在分享页页脚以纯文字出现，无 favicon。
2. AI 原生简洁性不足：首页一屏塞 5 个内容块（标题/副标/表单卡/热门 chips/「来自路线库」大卡），更像传统机票搜索表单；「AI」仅出现在文案里，搜索按钮与品牌区无 AI 语义；首页大卡与路线库 tab 内容重复。
3. 细节：交换按钮用粗糙的 `⇅` 字符。

**本轮改动（已落地到 `prototype/mvp-mobile.html`）**：
1. **新增品牌 Logo**：设计「路线 + 中转节点」图形标（蓝→靛渐变圆角底 + 白色路线曲线 + 三个节点，中间节点 = 中转城市，呼应产品核心概念）+ 「MixTouring」字标（Mix 用品牌渐变）。落位：首页品牌区、AI 生成加载页、浏览器 favicon、分享落地页页脚。
2. **首页减负**：移除「来自路线库」重型大卡，替换为一行轻量引导「看看背包客实测走过的省钱路线 →」（跳转路线库 tab）；首页收敛为 品牌 → 一句话价值主张 → AI 搜索卡 → 热门远途 chips。
3. **强化 AI 语义**：副标改为「告诉 AI 起点和终点，它会替你找出直飞之外的省钱路线」；搜索按钮加 AI 星芒图标并改为「让 AI 生成省钱方案」；加载页顶部展示品牌 Logo（AI「思考」时的品牌时刻）。
4. **细节**：交换按钮换成清晰的双向箭头 SVG。

**影响范围**：仅 `prototype/mvp-mobile.html` 视觉与文案；交互链路、数据层、产品方案不变。

**下一步（待创始人反馈后继续迭代）**：可选方向包括搜索交互更对话化、方案卡视觉层级再精简、配色是否引入更具旅行感的点缀色等。

---

### v0.3.1 · 2026-08-31 · 移动端可交互版本（用户视角）

**背景**：在 v0.3 桌面原型基础上，创始人要求产出适配手机的可交互版本——面向真实用户视角，去掉原型标注与开发提示，可输入起终点获得方案，底部 tab 可跳转。

**交付物**：`prototype/mvp-mobile.html`（单文件，移动端 Web App 形态）。

**交互链路（全部可用）**：
- 搜索：输入出发地/目的地 → 「生成中」加载态 → 方案列表（直飞基准 + 多方案卡）；演示数据层内置 4 条 OD（北京→喀什、北京→乌鲁木齐、上海→昆明、广州→西安），未覆盖 OD 进入「路线建设中」空态并推荐热门路线——演示也遵守「无数据不出卡」
- 方案卡 → 方案详情（分段明细 / 风险四因子 / 中转可玩性）→ 分享落地页 / 实测回填（含成功态）/ 收藏
- 路线库 tab：筛选 → 模板详情 → 一键复用（预填搜索并直接生成）
- 我的 tab：匿名本地 ID、我的收藏、我的回填记录（localStorage 持久化，跨刷新保留）

**UI 决策**：采用 Apple 风格（iOS 分组列表、大标题、毛玻璃 tab bar、分段控件）；全站字体统一为系统字体栈（-apple-system / PingFang SC / Microsoft YaHei 等），不再混用衬线与等宽字体；去除 v0.3 原型中的票据/手帐装饰与全部标注。

**影响范围**：新增交付物；产品方案不变。v0.3 桌面原型保留作为 PRD 配套说明图。

---

### v0.3 · 2026-08-30 · MVP 产品文档与原型图交付

**讨论背景**：第 2 轮剩余三议题（Q4 账号体系 / Q5 种子路线来源 / 询价 API 选型）应创始人要求暂停，先把已定稿内容收敛为可开发交付物。

**交付物**：
- `MVP产品文档.md`（v0.3）：需求背景、MVP 目标与双验收、范围表（含/不含及原因）、功能需求详述（S1–S7：搜索输入 / 方案生成 / 方案列表 / 方案详情与风险透明 / 路线库 / 分享 / 实测回填）、AI 引擎专项（准确率定义 / 评估体系 / 兜底降级 / 缓存成本）、数据与合规注意。
- `prototype/mvp-prototype.html`：中保真 Web 原型，7 个界面与 PRD §4 一一对应；含「生成中」骨架屏、「价格待补」降级卡、回填提交成功态等状态演示。

**文档化过程中固化的设定（此前未明确，随 v0.3 定稿）**：
1. 账号体系未决策 → MVP 默认按「匿名 + 本地保存（浏览器标识）」实现，标注 TBD，Q4 决策后支持迁移
2. 风险分不做黑箱总分：四个因子（衔接余量 / 行李直挂 / 退改规则 / 接驳距离）各自展示原始值与判级理由
3. 模板「一键复用」只复用路线结构（中转城市 + 交通方式），按用户日期窗重新询价，不锁定历史价格
4. 分享落地页无任何购买入口，主 CTA 为「按同样条件搜我的」（预填 OD 跳搜索页）

**影响范围**：新增两个交付物；产品方案与路线图不变。第 2 轮剩余待决策不变：Q4 账号体系 / Q5 种子路线来源 / 询价 API 选型。

---

### v0.2.2 · 2026-08-30 · 第 2 轮决策②：社区形态定稿——模板库 × 体验笔记双轨并行

**讨论背景**：第 2 轮待决策 Q2「社区走路线模板库（工具型、结构化、可复用）还是游记攻略流（内容型、传播强但沉淀弱）」。创始人决策：**不做二选一，双轨并行、两层分工**。

**创始人决策（采纳为定稿）**：
1. **路线模板库 = 工具层**：结构化、可复用，承接「搜索 → 方案 → 复用」的工具闭环（对应 M4 路线库），承担工具性用途
2. **攻略 / 体验笔记 = 内容层**：两个职能——**提高用户活跃度**、**体现产品品味**（调性即筛选，面向泛背包客的审美获客）

**讨论中补充的设计要点（随定稿一并记录）**：
3. **锚定关系**：每篇体验笔记必须挂在一条路线模板/方案卡上——内容流不散，规避「内容型沉淀弱」的弊病，模板也因此获得「实测故事」背书
4. **品味机制**：笔记层早期走**策展制**（官方精选 + 邀约供稿），不做全量 UGC 灌水；模板层可全量 UGC（结构化字段约束 + 实测数据背书）
5. **飞轮不变**：笔记中的实测花费 / 衔接顺利度以结构化字段回填，反哺引擎数据层（M5 机制延伸到笔记层）

**落地顺序**：模板库随 MVP 先行（已有 M4/M5 最小闭环）；笔记层在 Phase 2「社区构建」起量，冷启动供稿与 Q5（种子路线来源）联动决策。

**风险与注意**：双轨 = 两条内容线，早期精力有限 → 笔记层克制规模、宁缺毋滥；「活跃度」与「实测回流率」分开考核，避免为活跃牺牲方案质量（北极星仍是方案可用率）。

**影响范围**：产品方案 §8.2 支柱二（双轨结构）与模块表（新增 M7 体验笔记 · P2）、§8.7 Phase 2、§9.1 决策表、§9.2 剩余问题；第 2 轮待决策剩余 Q4 账号体系 / Q5 种子路线来源 / 询价 API 选型。

---

### v0.2.1 · 2026-08-30 · 第 2 轮决策①：AI 技术路线定稿

**讨论背景**：第 2 轮待决策 Q1「LLM 混合 vs 纯规则」。讨论中给出的推荐为「LLM 选路 + 数据校验」混合架构（数字只出自数据层、无数据不出卡）；创始人在此基础上提出了更具体的实现方案并被采纳。

**创始人方案（采纳为定稿）**：
1. **第一轮候选筛选**：AI 对起点和终点之间**地理上的中间城市 + 普遍交通枢纽**做第一轮选择（而非 LLM 自由提名）
2. **询价**：接入 OTA 或第三方平台询价（数据接入，非交易，与「不做买票服务」决策兼容）
3. **多方案生成**：AI 综合**价格 / 耗时 / 中转城市可玩性**给出多个方案——「可玩性」作为正式评分维度入卡（呼应回旋镖/中转顺便玩场景）
4. **AI 接入方式**：大模型能力以 API 方式接入（类似调用智能体），不自建模型

**与纯讨论的推荐相比的改进**：地理+枢纽预筛选收敛了 LLM 候选空间，降低幻觉与询价调用量；「可玩性」维度使方案卡从「交通方案」升级为「旅行方案」。

**连带决策**：第 2 轮 Q3（价格数据来源）一并解决——以 OTA/第三方询价 API 为主、票价样本库兜底；12306 无官方开放 API，铁路段用公布票价规则推算 + 社区实测校准。

**落地注意**：询价结果按 OD+日期窗缓存；铁路票价走公开定价规则；询价 API 选型（航旅纵横/飞常准等开放平台 vs OTA 开放平台）待技术验证。

**影响范围**：产品方案 §8.3 引擎流水线、§9 决策表；第 2 轮待决策剩余 Q2 社区形态 / Q4 账号体系 / Q5 种子路线来源。

---

### v0.2 · 2026-08-30 · 第 1 轮共创决策落地

**讨论背景**：v0.1 方案（搜索引擎 + CPS 导流）交付后，创始人对 4 个关键决策点作出选择，并明确产品初期重心。

**创始人决策**：
1. 市场范围：**预留国际架构**（引擎按国际通用票段模型设计，数据先接国内）
2. 产品形态：**Web 先行**（非小程序/独立 App）
3. 交易模式：**只提供内容和方案，不提供任何买票相关服务**
4. 首批人群：**泛背包客**（非仅限大学生）
5. 附加决策：初步版本聚焦 **AI 规划方案可用且好用 + 用户社区构建**；出票、佣金等商业化暂不考虑

**修改内容**（v0.1 → v0.2）：
- 定位：「省钱中转路线搜索引擎 + CPS 导流」→「背包客的 AI 省钱路线规划师 + 路线分享社区，纯内容与方案产品，不做交易」
- 产品重心：单一工具 → 双支柱（AI 规划引擎 × 用户社区）
- 功能模块：M1–M6 重构；移除「低价监控」「学生模式」为独立模块（降级/后置），新增 M4 路线库、M5 分享与实测（社区模块）
- MVP：新增验收用例②「10–20 名种子背包客盲测，可用率 ≥80%、采纳意愿 ≥50%」；价格从「可购票价」改为「近期估算区间」；MVP 即含社区最小闭环（分享链接 + 实测回填）
- 商业模式：CPS/订阅/保险等全部标记为「现阶段不做，远期存档选项」
- 风险：新增 AI 幻觉风险（LLM 编造车次/价格 → 结构化数据校验对策）、估算偏差风险、社区冷启动风险
- 路线图：P1「AI 引擎好用」→ P2「社区构建」→ P3「国际与探索」

**影响范围**：定位 / 功能模块 / MVP 范围 / 商业模式 / 路线图（全面更新）

**新一轮待决策（Q1–Q5）**：AI 技术路线（LLM 混合 vs 纯规则）/ 社区形态（模板库 vs 攻略流）/ 价格数据来源 / 用户账号体系 / 种子路线来源。

---

### v0.1 · 2026-08-30 · 市场调研 + 初步产品方案

**完成事项**：完成国内外市场调研，产出《市场调研 × 初步产品方案 v0.1》（`budget-route-app-research/budget-route-app-research.html`）。

**调研核心结论**：
1. 中转/联程玩法比直飞便宜 30%–50%（飞猪回旋镖机票单航段均价低 44%），省钱行为已被验证且规模化存在。
2. 国内 OTA 中转能力均为「系统推荐制」，无人支持「自选中转城市 × 跨交通方式 × 日期窗口」自由组价比价；官方空铁联运仅覆盖 40+ 城市 → 存在结构性空白。
3. 国际上 Kiwi.com（虚拟联程，日均 1 亿次搜索）、Skiplagged（弃程票，2025 年美法院裁定合法但有版权争议）、Omio/Rome2Rio（多式联运比价）模式已跑通，可移植机制：日期矩阵、灵活目的地反查、异构票段拼接、风险透明化。
4. 市场基本面：2025 年国内出游 65.22 亿人次 / 6.30 万亿元；特种兵旅游退潮但穷游逻辑常态化（人均日花费 269 元）；大学生预算 54.7% 集中在 2000–4000 元，交通为最大支出；低成本航空份额仅 12.5%（美国 35%）有增量；2025-09 学生票新政放宽。

**产品方案 v0.1 要点**：
- 定位：预算旅行者的「省钱中转路线搜索引擎」——自动发现直飞之外的火车×飞机自由组合，按总花费排序，衔接风险透明化。
- 六大功能模块：M1 路线引擎 / M2 总价视图 / M3 风险评分 / M4 预算反查 / M5 低价监控 / M6 学生模式。
- MVP 验收标准：输入「北京→喀什、国庆、预算优先」，能自动重现创始人的人工路线。
- 商业模式（由轻到重）：CPS 导流 → 增值订阅 → 续程保险分成 → B 端 API。
- 路线图：P1 MVP（0–3 月）→ P2 增长（3–9 月）→ P3 平台化（9 月+）。

**待决策问题（Q1–Q6）**：MVP 国内 or 预留国际 / App or 小程序 or Web / 跳转出票 or 自建出票 / 数据合规路径 / 是否纳入目的地内容 / 先打大学生还是泛背包客。

**本次修改内容**：（初始版本，无修改）

---

## 2. 修改记录模板（后续讨论后追加）

<!--
### v0.x · YYYY-MM-DD · [修改主题]
**讨论背景**：
**修改内容**：
- [修改点 1：旧 → 新，及理由]
**影响范围**：[定位/功能/MVP范围/商业模式/路线图]
**待办**：
-->
